# 용어 지도 — 모델이 서비스가 되기까지

녹음용 대본 — 총 533.73초 (60컷)

> 괄호 안 숫자는 그 문장을 읽는 데 배정된 시간입니다. 크게 벗어나면 영상과 어긋납니다.

## intro  (00:00.0 ~ 00:47.7)

- **[00:00.0]** 공장 전력 예측 모델을 만들었습니다. 그런데 내일 아침, 공장 담당자는 그 예측을 어떻게 받아 볼까요?  <sub>(8.6s)</sub>
  - 화면: 가운데 질문 문장 세 줄
- **[00:08.6]** 과제부터 짚겠습니다. 전날 자정까지의 기록과, 생산계획이나 기상 예보 같은 다음 날 계획으로, 다음 날 24시간의 시간별 평균전력을 예측합니다.  <sub>(12.4s)</sub>
  - 화면: 질문이 빠지고 과제 ① 전력 예측 칩 + 설명(기록 + 계획, 계획 = 생산계획 · 공장인원 · 기상 예보)
- **[00:21.1]** 그리고 15분 단위 피크 전력이 187킬로와트 이상이 될 위험한 시간을 골라냅니다.  <sub>(7.2s)</sub>
  - 화면: 과제 ② 피크 위험 칩 + 15분 피크 ≥ θ(회색 기준값)
- **[00:28.3]** 그런데 모델을 만들었다고 끝이 아닙니다. 오늘은 모델이 서비스가 되기까지 나오는 말들을 먼저 정리합니다.  <sub>(9.1s)</sub>
  - 화면: srv_title(1, 제목, 부제)
- **[00:37.4]** 전체 흐름은 이 여섯 칸입니다. 학습, 번들 저장, 같은 모델 증명, 검증하며 읽기, 하루치 예측, 그리고 CLI와 API입니다.  <sub>(10.3s)</sub>
  - 화면: series_map(None) 여섯 칸 전체 + 위에 이 시리즈가 따라가는 흐름

## learn_infer  (00:47.7 ~ 01:57.0)

- **[00:47.7]** 첫 번째 짝은 학습과 추론입니다. 둘은 무엇이 다를까요?  <sub>(5.1s)</sub>
  - 화면: 구간 제목 + 편 표식, 가운데 질문
- **[00:52.8]** 학습이 뭘까요? 지난 기록에서 규칙을 찾아 모델을 만드는 일입니다. 비유하자면 기출문제로 공부하기죠. 우리 프로젝트에서는 노트북의 5번째 장이 맡았습니다.  <sub>(14.1s)</sub>
  - 화면: 정의 카드 training (비유 딱지)
- **[01:06.9]** 여기서 노트북은 컴퓨터가 아니라, 코드와 설명, 실행 결과를 한 파일에 담은 분석 문서입니다.  <sub>(7.8s)</sub>
  - 화면: 정의 카드 notebook
- **[01:14.7]** 추론은요? 다 만든 모델에 새 입력을 넣고 답을 받는 일, 비유하자면 새 시험지 풀기죠. 우리는 전날까지의 기록과 다음 날 계획으로 다음 날 하루를 예측합니다.  <sub>(13.2s)</sub>
  - 화면: 정의 카드 inference (비유 딱지)
- **[01:27.8]** 우리 값으로 볼까요? 성적을 낸 모델은 2021년 1월 8일부터 8월 31일 23시까지, 한 시간에 한 줄씩, 5,599행의 데이터로 학습했습니다.  <sub>(13.4s)</sub>
  - 화면: [과거 데이터] →학습→ [모델], 아래 5,599행 · 기간
- **[01:41.2]** 추론은 매일 밤 한 번 돌리도록 만들었습니다. 새 입력을 넣으면 24행의 예측이 나옵니다.  <sub>(7.8s)</sub>
  - 화면: [새 입력] → [모델] → [예측 24행] (아래 줄)
- **[01:49.0]** 학습은 노트북에서 끝내고, 서비스는 학습 없이 추론만 매일 합니다. 이 구분이 출발점입니다.  <sub>(8.1s)</sub>
  - 화면: 두 줄 옆에 학습 노트북 / 추론 서비스 매일 칩

## model  (01:57.1 ~ 03:23.1)

- **[01:57.1]** 그럼 모델이란 뭘까요? 값을 넣으면 예측을 계산해 내주는 규칙 덩어리라고 보면 됩니다.  <sub>(7.5s)</sub>
  - 화면: 구간 제목 + 정의 카드 model
- **[02:04.5]** 우리 모델의 벽돌은 결정 트리, 예 아니오 질문을 차례로 던지는 규칙 나무입니다.  <sub>(6.6s)</sub>
  - 화면: 정의 카드 tree
- **[02:11.1]** 예를 들어 이런 나무입니다. 생산계획이 있는지 묻고, 있으면 기온이 25도보다 높은지 다시 물어, 닿은 잎의 값을 냅니다.  <sub>(10.4s)</sub>
  - 화면: 작은 결정 트리 그림 + 예시 칩 (질문 두 개, 잎 세 개)
- **[02:21.6]** 우리가 쓴 모델 도구 LightGBM은, 일반적으로 이런 나무 여러 그루의 답을 더해 예측 하나를 만듭니다.  <sub>(7.9s)</sub>
  - 화면: 나무 옆에 작은 나무 아이콘 여러 개 + 더하기 → 예측
- **[02:29.5]** 우리 모델은요? 전력 값을 맞히는 모델 하나에 나무 800그루, 모델 파일 전체로 10,800그루. 나머지 파일까지 합치면 25.4메가바이트입니다.  <sub>(14.2s)</sub>
  - 화면: fact_row 세 줄: 전력 값을 내는 모델 하나 800 · 모델 파일 전체 10,800 · 나머지 파일까지 합친 전체 크기 25.4 MB
- **[02:43.7]** 피처는 모델이 들여다보는 입력 값 하나하나입니다. 우리 모델은 한 시간마다 44개를 받죠.  <sub>(7.9s)</sub>
  - 화면: 정의 카드 feature
- **[02:51.6]** 예를 들어 2021년 9월 13일 월요일 오후 2시 줄입니다. 시각 14, 요일 번호 0은 월요일, 생산계획은 1018입니다.  <sub>(11.5s)</sub>
  - 화면: 표 머리 9월 13일 월 14시 행 — 44칸 중 6칸 + hour·dow·prod 세 칸
- **[03:03.1]** 어제 같은 시각 전력은 25킬로와트, 지난주 같은 시각은 160킬로와트, 기온은 24.2도입니다.  <sub>(9.3s)</sub>
  - 화면: y_avg_lag24 · y_avg_lag168 · temp 세 칸 추가
- **[03:12.4]** 어제 9월 12일은 생산이 없던 휴무일, 그 시각 전력도 25킬로와트로 낮았습니다. 이런 칸 44개가 모델의 입력입니다.  <sub>(10.6s)</sub>
  - 화면: y_avg_lag24 칸 강조(노랑) + 어제 = 생산이 없던 휴무일 주석

## memory_save  (03:23.1 ~ 04:40.4)

- **[03:23.1]** 다 만든 모델은 어디에 있을까요? 메모리, 프로그램이 도는 동안만 쓰는 작업 공간입니다. 비유하자면 수업이 끝나면 지워지는 칠판이죠.  <sub>(11.7s)</sub>
  - 화면: 구간 제목 + 정의 카드 memory (비유 딱지)
- **[03:34.8]** 그래서 따로 저장하지 않은 모델은, 노트북을 끄는 순간 메모리와 함께 사라집니다.  <sub>(6.8s)</sub>
  - 화면: [노트북 실행 중 | 메모리 안에 모델] → 노트북을 끔 → 메모리가 빔
- **[03:41.6]** 그래서 직렬화가 필요합니다. 메모리 속 모델을 파일에 적을 수 있게 바꾸는 일, 비유하자면 칠판을 공책에 옮겨 적기죠. 거꾸로 되살리는 건 로드입니다.  <sub>(12.8s)</sub>
  - 화면: 정의 카드 serialize (비유 딱지: 칠판을 공책에 옮겨 적기)
- **[03:54.4]** 그런데 모델 파일 하나로는 부족합니다. 모델 파일과 설정을 한 폴더에 모은 것, 이것이 번들입니다. 비유하자면 이삿짐 상자죠. 실행 코드는 따로 있고요.  <sub>(13.4s)</sub>
  - 화면: 정의 카드 bundle (비유 딱지)
- **[04:07.8]** 매니페스트는 그 상자에 무엇이 들었고 어떻게 써야 하는지 적은 문서입니다. 비유하자면 상자 겉에 붙인 짐 목록이죠.  <sub>(9.6s)</sub>
  - 화면: 정의 카드 manifest (비유 딱지)
- **[04:17.4]** 성적을 낸 모델의 번들을 열어 볼까요? 모델 파일 13개, 보정표와 점검용 사례를 담은 JSON 2개, 짐 목록 매니페스트까지 모두 25.4메가바이트입니다.  <sub>(13.6s)</sub>
  - 화면: 폴더 상자(bundle_id) 안에 파일 15개 + manifest.json 칩(노랑) + 아래 한 줄 요약
- **[04:31.0]** 짐 목록에는 피처 44칸의 순서, 경보 기준, 파일마다의 지문, 그리고 학습 때 쓴 도구들의 버전이 적혀 있습니다.  <sub>(9.4s)</sub>
  - 화면: manifest.json 안의 네 항목(피처 계약 · 임계값 τ · 파일 지문 · runtime)

## serve  (04:40.4 ~ 05:29.9)

- **[04:40.4]** 이제 모델을 내놓을 차례입니다. 서빙이란, 사람이든 다른 프로그램이든 필요할 때 모델에 예측을 맡길 수 있게 열어 두는 일입니다.  <sub>(10.8s)</sub>
  - 화면: 구간 제목 + 정의 카드 serving
- **[04:51.2]** 우리 서비스는 배치 방식, 정해 둔 때에 모아서 한꺼번에 처리합니다. 매일 밤 자정에 다음 날 24시간을 한 번에 예측하도록 만들었죠.  <sub>(11.1s)</sub>
  - 화면: 정의 카드 batch
- **[05:02.3]** 흐름을 그려 보면, 매일 자정에 두 가지 입력이 들어옵니다.  <sub>(5.0s)</sub>
  - 화면: 왼쪽 매일 24:00 → 이력 + 계획 상자
- **[05:07.3]** 하나는 이력, 전날 23시에서 끝나는 시간별 기록입니다. 최소 8일, 권장 14일이 필요합니다.  <sub>(8.8s)</sub>
  - 화면: 이력 패널: 전날 23시까지 · 최소 8일 · 권장 14일 · 전력·생산량·인원·기상 등
- **[05:16.1]** 다른 하나는 계획, 다음 날 24시간의 생산계획과 공장 인원, 그리고 기상 예보입니다.  <sub>(7.3s)</sub>
  - 화면: 계획 패널: 다음 날 24행 · 생산계획 · 공장인원 · 기상 예보
- **[05:23.4]** 서빙 코드가 번들을 읽어 이 두 입력으로 예측하면, 다음 날 24행이 나옵니다.  <sub>(6.5s)</sub>
  - 화면: → [번들 + 서빙 코드] → [예측 24행](처음부터 초록)

## doors  (05:29.9 ~ 06:28.6)

- **[05:29.9]** 예측을 받아 가는 문은 두 개입니다. 첫째 CLI는, 터미널이라는 글자 창에 명령을 한 줄 쳐서 프로그램을 돌리는 방식이죠.  <sub>(9.9s)</sub>
  - 화면: 구간 제목 + 정의 카드 cli
- **[05:39.8]** predict 명령에 이력과 계획 파일을 주면, 24행짜리 CSV 파일이 나옵니다. CSV는 엑셀로도 열리는 표 파일이죠.  <sub>(9.3s)</sub>
  - 화면: 터미널 모양 상자: python -X utf8 -m serving predict ... + 실제 OK 출력 → pred.csv (24행) + CSV 정의 한 줄
- **[05:49.1]** 둘째는 API, 다른 프로그램이 말을 거는 창구입니다. 약속한 모양으로 웹 주소에 요청하면, 약속한 모양의 답이 옵니다.  <sub>(10.0s)</sub>
  - 화면: 정의 카드 api
- **[05:59.1]** 그 창구 안에는 용도별 주소가 따로 있습니다. 이 주소 하나하나가 엔드포인트고, 우리 API에는 세 개가 있습니다.  <sub>(9.3s)</sub>
  - 화면: 정의 카드 endpoint
- **[06:08.4]** 헬스 체크 주소는 API가 살아 있는지, v1 model 주소는 모델 정보를 알려 주고, 예측 주소가 이력과 계획을 받아 24행을 돌려줍니다.  <sub>(10.7s)</sub>
  - 화면: 세 줄 표: GET /healthz · GET /v1/model · POST /v1/predict/day-ahead + 뜻, 아래 한 줄 GET = 조회 · POST = 데이터 보내기
- **[06:19.1]** 두 문은 같은 번들의 같은 예측 함수를 부릅니다. 명령 한 줄로 돌리면 CLI, 다른 프로그램이 주소로 부르면 API입니다.  <sub>(9.5s)</sub>
  - 화면: CLI · API 두 문 → 같은 번들 predict_day

## trust  (06:28.6 ~ 07:41.8)

- **[06:28.6]** 그런데 이 파일이 정말 그 모델일까요? 믿고 쓰게 해 주는 여섯 단어를 봅시다.  <sub>(6.5s)</sub>
  - 화면: 구간 제목 + 가운데 질문
- **[06:35.2]** 해시는 파일의 지문입니다. 내용으로 64자리 값을 계산하는데, 한 글자만 달라도 지문이 알아볼 수 없을 만큼 달라지죠.  <sub>(10.2s)</sub>
  - 화면: term_line hash
- **[06:45.4]** 무결성은 파일이 처음 만들어진 모습 그대로, 깨지거나 누가 고치지 않았다는 뜻입니다.  <sub>(7.2s)</sub>
  - 화면: term_line integrity
- **[06:52.5]** 결정성은 입력과 설정, 환경이 같으면 결과도 늘 같다는 성질이고,  <sub>(5.2s)</sub>
  - 화면: term_line determinism
- **[06:57.8]** 재현성은 시간이 지나 다시 해 봐도 그 결과를 그대로 다시 얻을 수 있다는 뜻입니다.  <sub>(6.5s)</sub>
  - 화면: term_line reproducibility
- **[07:04.3]** 자가검증은 번들을 읽어 들일 때마다, 미리 계산해 둔 답이 그대로 나오는지 같은 계산을 다시 해 보는 것입니다.  <sub>(8.8s)</sub>
  - 화면: term_line selftest
- **[07:13.1]** 자동 테스트는 코드가 약속대로 움직이는지 대신 확인해 주는, 검사용 작은 프로그램들입니다.  <sub>(7.7s)</sub>
  - 화면: term_line test
- **[07:20.8]** 이 말들은 3편부터 7편까지, 실제 번들과 실험 결과로 직접 확인합니다.  <sub>(6.2s)</sub>
  - 화면: 각 줄 오른쪽에 몇 편에서 보는지 칩(3~5편, 3~5편, 3, 4편, 4편, 4, 5편, 7편 — 가운뎃점 대신 쉼표)
- **[07:27.0]** 짚고 갈 점입니다. 모델 파일만으로는 서비스가 안 됩니다. 경보 기준 타우, 피처 44칸의 순서를 지키자는 약속인 피처 계약, 그리고 휴일표까지 있어야 같은 예측이 나옵니다.  <sub>(14.8s)</sub>
  - 화면: 목록이 빠지고 watch_card

## map  (07:41.8 ~ 08:19.1)

- **[07:41.8]** 지도를 다시 봅시다. 첫 칸 학습은 노트북 5번째 장의 일이고, 이 시리즈는 그다음부터입니다.  <sub>(8.1s)</sub>
  - 화면: series_map(None) 위쪽, 첫 칸만 회색 + 서빙 01 줄
- **[07:49.9]** 번들 저장은 두 편입니다. 2편은 왜 파일로 남기는지, 3편은 15개 파일과 짐 목록 만들기.  <sub>(8.1s)</sub>
  - 화면: 번들 저장 칸 노랑 + 아래 목록 서빙 02 · 서빙 03
- **[07:58.0]** 4편은 저장한 모델이 제출한 모델과 정말 같은지 증명합니다.  <sub>(5.2s)</sub>
  - 화면: 같은 모델 증명 칸 노랑 + 서빙 04
- **[08:03.2]** 5편은 코드 한 벌을 나눠 쓰는 법과, 번들을 검사하며 읽는 과정.  <sub>(5.4s)</sub>
  - 화면: 검증하며 읽기 칸 노랑 + 서빙 05
- **[08:08.6]** 6편은 9월 13일 하루치 예측이 만들어지는 길을 따라갑니다.  <sub>(5.5s)</sub>
  - 화면: 하루치 예측 칸 노랑 + 서빙 06
- **[08:14.1]** 마지막 7편은 CLI와 API로 내보내는 법과 운영 이야기입니다.  <sub>(5.0s)</sub>
  - 화면: CLI · API 칸 노랑 + 서빙 07

## outro  (08:19.1 ~ 08:53.7)

- **[08:19.1]** 정리해 볼까요. 학습은 과거에서 규칙을 찾는 일, 추론은 새 입력에 답하는 일입니다.  <sub>(7.3s)</sub>
  - 화면: 구간 제목 정리 + 편 표식 + recap_card 1줄
- **[08:26.4]** 모델은 메모리에 있다가 사라지므로, 직렬화해서 파일로 남깁니다.  <sub>(5.7s)</sub>
  - 화면: recap 2줄
- **[08:32.1]** 번들은 모델 파일과 설정을 모은 꾸러미이고, 매니페스트가 그 짐 목록입니다.  <sub>(6.4s)</sub>
  - 화면: recap 3줄
- **[08:38.6]** 서빙은 매일 자정의 배치 예측이고, 받아 가는 문은 CLI와 API 두 개입니다.  <sub>(5.9s)</sub>
  - 화면: recap 4줄
- **[08:44.5]** 오늘 용어는 영상 폴더의 리드미 용어집에도 모아 두었습니다. 다음 편은 모델을 왜 파일로 남기는지부터 봅니다.  <sub>(9.3s)</sub>
  - 화면: 용어집: 영상 폴더 README 줄 + related_card 서빙 02
