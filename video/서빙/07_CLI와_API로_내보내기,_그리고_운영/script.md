# CLI와 API로 내보내기, 그리고 운영

녹음용 대본 — 총 533.64초 (53컷)

> 괄호 안 숫자는 그 문장을 읽는 데 배정된 시간입니다. 크게 벗어나면 영상과 어긋납니다.

## intro  (00:00.0 ~ 00:40.5)

- **[00:00.0]** 예측 함수는 이제 준비됐습니다. 그런데 공장의 다른 시스템은, 이 예측을 어떻게 불러서 쓸까요?  <sub>(8.3s)</sub>
  - 화면: 질문 문장이 가운데 써진다
- **[00:08.3]** 과제부터 짚죠. 전날 자정에 다음 날 24시간의 평균전력과, 15분 피크가 187킬로와트 이상인 위험 시간을 내놓는 것입니다.  <sub>(11.2s)</sub>
  - 화면: 과제 A(평균전력 24시간) · 과제 B(peak15 ≥ θ) 두 줄 칩
- **[00:19.4]** 오늘은 3부의 마지막 편, 이 예측을 밖으로 내보내고 운영하는 이야기입니다.  <sub>(6.5s)</sub>
  - 화면: 편 제목 카드 srv_title(7)
- **[00:25.9]** 지도로 보면 학습부터 하루치 예측까지 왔고, 이제 마지막 칸, CLI와 API입니다.  <sub>(6.5s)</sub>
  - 화면: e2e 지도, 여섯째 칸(CLI · API) 강조
- **[00:32.4]** 문은 두 개입니다. 매일 자정에 예약해 돌리는 CLI, 다른 프로그램이 필요할 때 부르는 API입니다.  <sub>(8.1s)</sub>
  - 화면: 번들 상자 → 두 문: CLI(매일 24:00 예약 실행 → 결과 파일) / API(요청 → 응답)

## cli  (00:40.5 ~ 02:03.4)

- **[00:40.5]** 첫 번째 문, CLI부터 보죠. 마우스 대신, 터미널 창에 명령어를 글자로 쳐서 프로그램을 쓰는 방식입니다.  <sub>(8.9s)</sub>
  - 화면: 구간 제목 + 정의 카드 cli
- **[00:49.4]** 명령은 세 개입니다. predict는 다음 날 24행을 CSV, 곧 쉼표로 칸을 나눈 표 파일로 쓰고, replay는 과거를 다시 재생, verify는 번들 검사만 합니다.  <sub>(12.0s)</sub>
  - 화면: 명령 세 줄: python -m serving predict / replay / verify + 뜻 · 아래에 CSV 한 줄 정의
- **[01:01.4]** 자정에 사람 없이 돈 명령이 성공했는지는 어떻게 알까요? 끝날 때 남기는 숫자, 종료코드입니다. 0이면 성공이죠.  <sub>(10.1s)</sub>
  - 화면: 정의 카드 exit_code
- **[01:11.5]** 실패는 종류별로 나눴습니다. 1은 예측 실패, 2는 입력 오류, 3은 번들 오류. 숫자만 봐도 어느 쪽 문제인지 압니다.  <sub>(10.5s)</sub>
  - 화면: 종료코드 사다리 0 · 1 · 2 · 3 + 오류 클래스 이름
- **[01:22.0]** 코드로 보면, predict 명령은 번들을 읽고 하루치를 예측해 파일로 쓴 뒤 0을 돌려줍니다.  <sub>(7.2s)</sub>
  - 화면: 코드 창 srv07_cli.py, 1~8줄 띠
- **[01:29.2]** 맨 바깥 함수는 서빙 오류를 붙잡아 오류 이름과 설명을 찍고, 오류 종류에 정해진 종료코드를 돌려줍니다.  <sub>(8.4s)</sub>
  - 화면: 띠가 main 의 except 블록(14~18줄)으로
- **[01:37.7]** 실제로 돌려 봤습니다. 정상 입력은 0. 계획에 다음 날 실제 전력을 넣으면, 미리 알 수 없는 답이 섞인 누수라서 입력 오류 2입니다.  <sub>(11.3s)</sub>
  - 화면: 실행 결과 두 줄(정상 0 · 계획에 전력 평균 2): 종료코드 칩 + stderr 원문 · 아래에 누수 한 줄 정의
- **[01:48.9]** 없는 번들 폴더로 검사 명령을 돌리면 3입니다.  <sub>(4.1s)</sub>
  - 화면: 셋째 줄: 번들 폴더 없음 (verify) → 3 + stderr 원문
- **[01:53.0]** 파일로 저장할 때는 맨 앞에 비오엠이라는 세 바이트 표식을 붙입니다. 일반적으로 엑셀은 이 표식을 보고 한글을 제대로 엽니다.  <sub>(10.4s)</sub>
  - 화면: EF BB BF + 첫 줄 머리(ts, y_avg_pred …) · --out 으로 저장한 파일 · BOM 한 줄 정의

## api_app  (02:03.4 ~ 03:24.5)

- **[02:03.4]** 두 번째 문, API입니다. 프로그램끼리 요청과 응답을 주고받는 약속된 창구죠. 비유하자면 식당 주문 창구입니다.  <sub>(9.9s)</sub>
  - 화면: 구간 제목 + 정의 카드 api (비유 딱지)
- **[02:13.3]** 주고받는 규칙은 HTTP입니다. 웹 주소로 요청하고 응답을 받죠. GET은 조회, POST는 데이터를 보내는 요청입니다.  <sub>(9.9s)</sub>
  - 화면: 정의 카드 http
- **[02:23.2]** API 안의 주소 하나하나를 엔드포인트라고 부릅니다. 우리 API에는 세 개가 있습니다.  <sub>(7.1s)</sub>
  - 화면: 정의 카드 endpoint
- **[02:30.3]** 첫째는 서버가 살아 있는지 묻는 헬스 체크 주소, 둘째 v1 model은 어떤 모델인지 알려 주고, 셋째 예측 주소에 지난 기록과 다음 날 계획을 POST로 보내면 24행이 옵니다.  <sub>(13.4s)</sub>
  - 화면: 주소 세 줄: GET /healthz(헬스 체크) · GET /v1/model · POST /v1/predict/day-ahead + 돌려주는 것
- **[02:43.7]** 주고받는 내용은 JSON입니다. 이름과 값의 쌍을 중괄호로 묶어 적는 글자 형식이죠.  <sub>(7.1s)</sub>
  - 화면: 같은 화면 아래에 JSON 한 줄 정의(TERMS json)
- **[02:50.8]** 이 API는 파이썬 라이브러리 FastAPI로 만들었습니다. 웹 API를 짧은 코드로 만드는 도구죠.  <sub>(7.5s)</sub>
  - 화면: 정의 카드 fastapi
- **[02:58.2]** 앱을 만드는 함수는 먼저 번들을 읽고 검증합니다. 이 PC에서 244밀리초. 실패하면 서버가 아예 뜨지 않습니다.  <sub>(10.0s)</sub>
  - 화면: 코드 창 srv07_app.py, 1~4줄 띠 + 오른쪽 설명 + 기동 시간(이 PC)
- **[03:08.2]** 요청은 하루 몇 번뿐이라 동시 처리가 필요 없습니다. 그래서 서버 프로그램인 워커는 하나만 띄우고, 잠금으로 예측을 하나씩 돌립니다.  <sub>(11.2s)</sub>
  - 화면: 띠가 21~22줄(with lock)로 + 7줄(lock = threading.Lock()) 두 번째 띠 → 설명 ② 워커 1개 · 잠금
- **[03:19.4]** FastAPI가 자동으로 만드는 사용 설명 페이지는 꺼 두었습니다.  <sub>(5.1s)</sub>
  - 화면: 13줄(docs_url=None) 띠 + 설명 ③

## call  (03:24.5 ~ 04:30.7)

- **[03:24.5]** 실제로 한 번 요청해 보죠. 대상일은 9월 14일, 이력은 14일치 336행, 계획은 24행, 본문은 72.5킬로바이트입니다.  <sub>(12.2s)</sub>
  - 화면: 구간 제목 + 요청 블록(POST 주소, 머리말 X-API-Key, JSON 뼈대) + 시연용 날짜 표시(회색)
- **[03:36.7]** 응답에 붙는 결과 번호가 상태코드입니다. 200은 성공, 4로 시작하면 요청 쪽, 5로 시작하면 서버 쪽 문제죠.  <sub>(9.9s)</sub>
  - 화면: 정의 카드 status
- **[03:46.6]** 이번 응답은 200, 성공입니다. 설명 정보인 meta에는 답한 번들, 이력 시작일과 일수, 경고, 요청 번호가, rows에는 24시간 예측이 담깁니다.  <sub>(12.8s)</sub>
  - 화면: 응답 · 상태 200 + meta 블록(role 포함, 값은 facts) + rows[24]
- **[03:59.4]** 14시 행은 평균 163.1, 피크 171.6킬로와트. 경보 기준 타우 172.23보다 조금 낮아 경보 라벨은 0입니다.  <sub>(12.4s)</sub>
  - 화면: 14시 행 칸 몇 개 + τ 비교
- **[04:11.8]** 같은 입력을 CLI로 돌린 파일과 0시, 14시 행의 모든 칸이 같습니다. 문은 둘, 예측은 하나입니다.  <sub>(8.7s)</sub>
  - 화면: CLI 파일 행 vs API 응답 행 ✔ (0시·14시)
- **[04:20.4]** 응답 시간은 다섯 번 잰 가운데 값이 103.6밀리초. 네트워크 없이 이 PC 안에서 시험용 클라이언트로 잰 값입니다.  <sub>(10.2s)</sub>
  - 화면: 응답 시간 fact_row (5회 중앙값 · 이 PC · 네트워크 없는 테스트 클라이언트)

## guard  (04:30.7 ~ 05:53.7)

- **[04:30.7]** 밖에서 요청이 들어오니 문지기가 필요합니다. 미들웨어, 모든 요청이 먼저 지나가는 검문소죠. 비유하자면 건물 입구 보안 검색대입니다.  <sub>(11.9s)</sub>
  - 화면: 구간 제목 + 정의 카드 middleware (비유 딱지)
- **[04:42.6]** API 키는 쓸 자격을 확인하는 비밀 문자열입니다. 요청 머리말에 실어 보냅니다.  <sub>(6.7s)</sub>
  - 화면: 정의 카드 api_key
- **[04:49.3]** 문지기는 차례로 봅니다. 크기를 밝히지 않은 POST는 크기 제한을 피할 수 있어 411, 2메가바이트를 넘으면 413, 키를 설정한 서버라면 키가 없거나 틀릴 때 401.  <sub>(14.3s)</sub>
  - 화면: 검문 3단 도식: 길이 표시? → 2 MB 이하? → API 키?(키를 설정했을 때 · /v1/ 주소만) → 본 처리
- **[05:03.6]** 통과해도 입력 모양을 또 봅니다. 모르는 필드, 영하 60도에서 60도를 벗어난 기온, 숫자가 아닌 값이나 무한대는 거부합니다.  <sub>(11.1s)</sub>
  - 화면: 도식 아래 입력 모양 규칙 세 줄
- **[05:14.7]** 일부러 틀린 요청을 보내 봤습니다. 키 없이 보내면 401, 계획에 평균 전력, 이력 5일, 기온 999도는 모두 422, 입력이 틀렸다는 뜻이죠.  <sub>(12.8s)</sub>
  - 화면: 실제 결과 표 앞 4행(401 · 422 × 3)
- **[05:27.6]** 예측 주소에 조회용 GET은 405, 없는 주소는 404, 상한을 1,000바이트로 낮춘 시험 서버에서는 413이었습니다.  <sub>(10.9s)</sub>
  - 화면: 표 나머지 3행(405 · 404 · 413, 상한 1,000바이트 시험 앱)
- **[05:38.5]** 오류가 나면 서버는 늘 같은 모양으로 답합니다. 비유하자면 겉봉이 같은 편지, 그래서 오류 봉투라고 부릅니다. 안에는 오류 이름, 설명, 필요할 때만 상세, 요청 번호가 들어 있죠.  <sub>(15.2s)</sub>
  - 화면: 오류 봉투 한 줄 정의 + 비유 딱지 + 오류 봉투 JSON (error_example) + detail 은 필요할 때만

## ops  (05:53.7 ~ 07:15.5)

- **[05:53.7]** 이제 운영입니다. 먼저 로그, 프로그램이 언제 무엇을 했는지 남긴 기록입니다. 비유하자면 업무 일지죠.  <sub>(9.3s)</sub>
  - 화면: 구간 제목 + 정의 카드 log (비유 딱지)
- **[06:03.0]** API는 요청마다 로그를 JSON 한 줄로 남깁니다. 언제, 어느 주소로, 결과가 어땠는지. 보낸 입력값은 남기지 않습니다.  <sub>(10.3s)</sub>
  - 화면: 로그 한 줄(키 6개) + '입력값은 남기지 않는다'
- **[06:13.4]** 다음은 롤백, 문제가 생기면 이전 버전으로 되돌리는 것입니다. 비유하자면 게임의 세이브 파일을 불러오는 것이죠.  <sub>(9.7s)</sub>
  - 화면: 정의 카드 rollback (비유 딱지)
- **[06:23.1]** 분석 노트북을 처음부터 다시 돌리면 결과 폴더의 번들은 지워지고 새로 쓰입니다. 그래서 서버는 밖에 복사한, 바꾸지 않는 사본을 가리킵니다.  <sub>(11.7s)</sub>
  - 화면: outputs/models/full/deploy → 복사 → /srv/kamp/bundles/<bundle_id> ← 서버
- **[06:34.8]** 되돌릴 때는 이전 사본 경로로 바꾸고 다시 켭니다. 켤 때마다 번들 검사를 다시 하니, 되돌린 번들도 검사를 통과해야 뜹니다.  <sub>(10.3s)</sub>
  - 화면: 서버 화살표가 '이전 사본' 으로 옮겨감 + 재시작
- **[06:45.0]** 마지막은 휴일표, 공휴일 날짜 목록입니다. 모델은 그날이 휴일인지를 입력으로 받으니, 그해 공휴일을 알아야 예측합니다.  <sub>(10.5s)</sub>
  - 화면: 정의 카드 holiday
- **[06:55.6]** 학습 때 쓴 표와 따로, 운영자가 관리하는 운영 휴일표가 있습니다. 지금은 2021년 날짜 18개뿐입니다.  <sub>(9.5s)</sub>
  - 화면: holidays_kr.json: coverage_years [2021] · 18개 (학습용 표와 별도)
- **[07:05.1]** 그래서 2022년 날짜는 평일로 짐작하지 않고 입력 오류로 거부합니다. 새해는 운영자가 확인한 날짜를 추가해야 합니다.  <sub>(10.4s)</sub>
  - 화면: 2022-03-01 요청 → CALENDAR_NOT_COVERED · 422 · 종료코드 2 + 새해 추가 안내

## tests  (07:15.5 ~ 07:45.8)

- **[07:15.5]** 이 약속들이 계속 지켜지는지는 누가 볼까요? 자동 테스트, 코드가 기대대로 도는지 확인하는 작은 검사 프로그램입니다.  <sub>(10.1s)</sub>
  - 화면: 구간 제목 + 정의 카드 test
- **[07:25.5]** 서빙과 번들 테스트는 모두 181개, 그 가운데 단위와 코드 생성 테스트 123개를 이 PC에서 돌려 모두 통과했습니다.  <sub>(9.8s)</sub>
  - 화면: 가로 막대 5개(단위·코드 생성·리플레이·API·번들) + 합계 + 위 두 막대 '123 passed'
- **[07:35.4]** 다만 나머지 묶음은 모델을 작게 다시 만드는 축소 실행으로 결과 폴더를 덮어씁니다. 돌린 뒤엔 전체 실행을 한 번 더 해야 합니다.  <sub>(10.4s)</sub>
  - 화면: 주의 한 줄(회색)

## watch  (07:45.8 ~ 08:16.3)

- **[07:45.8]** 끝으로 짚고 갈 점입니다. 배포용 번들은 2021년 1월 초부터 9월 14일까지만 배웠습니다. 다른 달, 다른 해는 배운 범위 밖, 외삽이죠. 경보 기준과, 확률을 실제 비율에 맞추는 보정기도 평가용 번들에서 물려받았습니다.  <sub>(19.6s)</sub>
  - 화면: 짚고 갈 점 카드 + 본문 1·2줄 강조
- **[08:05.3]** 학습 때는 기상 실측을 예보 대신 썼으니, 운영에서는 예보가 틀린 만큼 나빠질 수 있습니다. 재학습하려면 노트북부터 고쳐야 합니다.  <sub>(11.0s)</sub>
  - 화면: 카드 본문 3·4줄 강조

## outro  (08:16.3 ~ 08:53.6)

- **[08:16.3]** 시리즈 전체를 돌아보죠. 과거 데이터로 모델을 학습하고, 그 모델을 번들로 저장했습니다.  <sub>(7.9s)</sub>
  - 화면: series_map(None), 1·2칸 밝힘
- **[08:24.2]** 같은 모델임을 증명하고, 검증하며 읽고, 하루치 예측을 만들어, 오늘 CLI와 API로 밖에 내보냈습니다.  <sub>(8.4s)</sub>
  - 화면: 3~6칸 차례로 밝힘
- **[08:32.6]** 정리하면, CLI는 종료코드로, API는 상태코드와 오류 봉투로 결과를 알립니다. 문지기와 입력 검사를 지나야 예측이 돌고, 운영은 불변 사본과 롤백, 휴일표로 지킵니다.  <sub>(13.9s)</sub>
  - 화면: recap_card 4줄
- **[08:46.5]** 오늘 나온 용어는 영상 폴더 README의 용어집에 있습니다. 함께해 주셔서 감사합니다.  <sub>(7.1s)</sub>
  - 화면: 용어집 안내 한 줄
