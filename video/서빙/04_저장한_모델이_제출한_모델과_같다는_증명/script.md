# 저장한 모델이 제출한 모델과 같다는 증명

녹음용 대본 — 총 499.45초 (49컷)

> 괄호 안 숫자는 그 문장을 읽는 데 배정된 시간입니다. 크게 벗어나면 영상과 어긋납니다.

## intro  (00:00.0 ~ 00:58.8)

- **[00:00.0]** 저장해 둔 모델이, 정말 제출 성적을 낸 바로 그 모델일까요? 여기 대회에 낸 예측 파일과, 모델을 저장한 번들 폴더가 있습니다.  <sub>(10.5s)</sub>
  - 화면: 왼쪽 상자 '제출한 예측 파일'(predictions_test_336h.csv · 테스트 14일 · 336행), 오른쪽 상자 '번들 폴더'(평가 번들 id · 파일 15개 · 25.4 MB)
- **[00:10.5]** 둘이 같다는 걸 어떻게 증명할까요?  <sub>(3.1s)</sub>
  - 화면: 두 상자 사이에 '= ?' 가 뜨고 아래에 질문 문장
- **[00:13.6]** 과제부터 짧게 짚죠. 전날 자정까지의 기록으로 다음 날 24시간 평균전력을 예측하고, 15분 피크가 187킬로와트 이상인 위험 시간을 골라냅니다.  <sub>(12.8s)</sub>
  - 화면: 과제 칩 두 줄: 평균전력 24시간 예측 / 15분 피크 ≥ θ 위험 시간
- **[00:26.4]** 오늘은 서빙 네 번째 편, 저장한 모델이 제출한 모델과 같다는 증명입니다.  <sub>(6.1s)</sub>
  - 화면: srv_title(4, …)
- **[00:32.5]** 먼저 재현성이란 말부터 볼까요? 한마디로 다시 해도 똑같이 나온다는 겁니다. 저장한 모델로 다시 예측하면 제출 파일과 같은 값이 나와야 하죠.  <sub>(12.0s)</sub>
  - 화면: 정의 카드 reproducibility (비유 딱지 포함)
- **[00:44.5]** 비유하자면 같은 레시피, 같은 재료로 다시 끓이면 같은 맛이 나는 거죠. 오늘은 이걸 네 가지 방법으로 확인합니다.  <sub>(9.5s)</sub>
  - 화면: 카드가 위로 올라가고(크기 그대로) 아래에 확인 방법 칩 네 개: 비트 비교 · 다시 학습 · 자가검증 · 리플레이
- **[00:54.0]** 전체 흐름에서는 세 번째 칸, 같은 모델 증명 단계입니다.  <sub>(4.8s)</sub>
  - 화면: series_map(2) — 세 번째 칸이 노랗게

## six_checks  (00:58.8 ~ 02:09.2)

- **[00:58.8]** 번들을 저장하는 곳은 분석 코드 문서, 곧 노트북의 10.5절입니다. 저장하자마자 점검 여섯 개를 돌리는데, 비트 비교와 다시 학습도 이 안에 들어 있죠.  <sub>(13.2s)</sub>
  - 화면: 구간 제목 + 표 머리(점검 · 무엇과 무엇을 비교하나 · 결과)
- **[01:12.1]** 첫째는 메모리의 모델과 파일에서 다시 읽은 모델을 끝자리까지, 둘째는 다시 읽은 모델을 제출 파일과 비교합니다.  <sub>(9.2s)</sub>
  - 화면: 1 eval_bitwise, 2 submission_match 두 행
- **[01:21.2]** 셋째는 같은 데이터로 처음부터 다시 학습해, 모델 파일의 지문이 같은지 봅니다.  <sub>(6.6s)</sub>
  - 화면: 3 refit_sha 행
- **[01:27.8]** 넷째, 서비스에 쓰는 배포 번들은 9월 14일까지 다시 학습한 별도 모델이라, 제출 파일 대신 메모리의 모델과 파일에서 다시 읽은 모델이 끝자리까지 같은지만 봅니다.  <sub>(13.4s)</sub>
  - 화면: 4 deploy_reload 행 + 아래 회색 한 줄(9월 14일까지 다시 학습한 별도 모델 · 제출 파일과는 비교하지 않음)
- **[01:41.2]** 다섯째는 매니페스트의 파일 지문이 실제 파일과 맞는지, 여섯째는 대회가 블라인드 심사라서, JSON 파일에 내 컴퓨터의 폴더 위치나 계정 이름이 없는지 봅니다.  <sub>(12.7s)</sub>
  - 화면: 5 integrity, 6 manifest_clean 두 행
- **[01:53.9]** 축소 없이 끝까지 돌린 실제 실행, 곧 전체 모드의 출력을 보면 여섯 개 모두 참, 통과입니다.  <sub>(7.4s)</sub>
  - 화면: 결과 열에 ✔ True 여섯 개 + 출처 캡션(노트북 FULL(전체 학습) 실행 출력 · 평가 번들 id)
- **[02:01.3]** 하나라도 거짓이면 노트북은 실패한 점검 이름을 띄우고 멈춰서, 그 뒤 단계로 넘어가지 않습니다.  <sub>(7.9s)</sub>
  - 화면: 표 아래 한 줄: 하나라도 False → AssertionError, 노트북 중단

## bitwise  (02:09.2 ~ 03:31.6)

- **[02:09.2]** 첫 번째 점검은 비트 단위로 같아야 통과합니다. 비트는 컴퓨터가 값을 저장하는 가장 작은 단위, 0 또는 1 한 자리죠.  <sub>(9.8s)</sub>
  - 화면: 구간 제목 + '비트 = 0 또는 1 한 자리' 짧은 정의 + 0/1 칸 한 줄(τ 의 실제 64비트)
- **[02:19.1]** 소수는 비트 여러 개를 모은 칸에 저장하는데, 이 칸의 크기가 부동소수점 정밀도입니다. 64비트가 32비트보다 훨씬 정밀하고, 우리 서빙은 모두 64비트를 씁니다.  <sub>(14.1s)</sub>
  - 화면: 정의 카드 float
- **[02:33.2]** 예를 들어 경보 기준 타우는 보통 172.23 이라고 부릅니다.  <sub>(5.9s)</sub>
  - 화면: 위에 τ 의 64비트 칸, 아래 '경보 기준 τ — 보통 이렇게 부른다 172.23'
- **[02:39.1]** 그런데 저장된 값을 파이썬으로 그대로 적으면 소수 열네째 자리에 2가 붙어 있습니다. 172.23 과는 다른 값이라, 매니페스트는 반올림 없이 그대로 적어 둡니다.  <sub>(14.2s)</sub>
  - 화면: 둘째 줄 '저장된 값을 파이썬이 그대로 적으면 172.23000000000002'(repr), 끝자리 2 강조 + 매니페스트 캡션
- **[02:53.3]** 비트 단위로 같다는 건 이런 끝자리까지 모두 같다는 뜻입니다. 테스트 336시간의 평균, 피크, 확률, 그리고 예측 범위의 아래, 가운데, 위까지 여섯 가지를 봅니다.  <sub>(14.2s)</sub>
  - 화면: 왼쪽 '메모리 속 모델', 오른쪽 '파일에서 다시 읽은 모델', 가운데 출력 여섯 가지 칩과 '='
- **[03:07.5]** 노트북 코드로는, 다시 읽은 번들로 테스트 기간을 예측하고,  <sub>(4.9s)</sub>
  - 화면: 코드 창 srv04_checks.py, 1~2줄 띠 + 오른쪽 설명
- **[03:12.3]** 넘파이의 배열 비교 함수로, 한 칸도 빠짐없이 같을 때만 참이 됩니다.  <sub>(5.7s)</sub>
  - 화면: 3~10줄 띠 + 설명 교체
- **[03:18.0]** 두 번째 점검은 다릅니다. 제출 파일은 평균과 피크가 소수 넷째 자리, 확률이 여섯째 자리로 반올림돼 있어서, 같은 자리로 반올림해 비교하고 경보 라벨도 맞춰 봅니다.  <sub>(13.6s)</sub>
  - 화면: 코드가 빠지고 예시: 9월 13일 14시 평균 예측 — 끝자리까지 값과 제출 파일 4자리 값(앞 자리 세로 정렬, 같은 앞자리 색) + 점검 2 규칙

## determinism  (03:31.6 ~ 04:39.7)

- **[03:31.6]** 세 번째 점검에는 3편에서 본 해시, 곧 파일의 지문이 나옵니다. 한 글자만 달라도 전혀 다른 값이 되죠.  <sub>(8.7s)</sub>
  - 화면: 구간 제목 + 정의 카드 hash (복습)
- **[03:40.3]** 결정성도 복습이죠. 같은 걸 넣고 같은 방식으로 돌리면 늘 같은 답이 나오는 성질입니다.  <sub>(7.5s)</sub>
  - 화면: 정의 카드 determinism (복습 칩)
- **[03:47.8]** 조건은 세 가지입니다. 같은 데이터, 곧 8월 31일까지의 5,599행. 같은 설정, 곧 동시에 도는 계산 줄기인 스레드를 4개로 묶고, 결과가 흔들리지 않게 하는 결정적 모드를 켭니다.  <sub>(16.2s)</sub>
  - 화면: 세 조건 중 두 상자: 같은 데이터(기간·행수) · 같은 설정(deterministic=True · num_threads=4) + 코드 주석 인용
- **[04:04.0]** 그리고 같은 시드. 시드는 무작위로 고르는 과정의 출발 번호인데, 42로 고정합니다.  <sub>(7.6s)</sub>
  - 화면: 셋째 상자 같은 시드(seed=42) + 화살표 → 모델 파일 13개 '다시 학습해도 바이트까지 같았다'
- **[04:11.6]** 코드는 같은 기준 시점으로 처음부터 다시 학습하고, 모델 파일 13개의 지문을 저장한 지문과 통째로 비교합니다.  <sub>(9.3s)</sub>
  - 화면: 코드 창 srv04_checks.py 다시, 12~18줄 띠 + 오른쪽 설명
- **[04:20.9]** 결과는 참. 13개 파일의 지문이 모두 같았습니다. 화면에는 앞 열두 자리만 적었습니다.  <sub>(8.1s)</sub>
  - 화면: 점 lgb 파일 13개 이름 + 지문 앞 12자리 + ✔
- **[04:29.0]** 그러니 같은 환경이라면, 모델 파일을 잃어도 같은 데이터와 코드로 바이트까지 같은 파일을 다시 만들 수 있고, 지문으로 확인됩니다.  <sub>(10.7s)</sub>
  - 화면: 가운데 세 줄: 같은 환경이라면 · 모델 파일을 잃어도 / 같은 데이터 + 같은 코드 → 바이트까지 같은 파일 / 지문으로 확인 + 아래 '같은 환경 =' 줄(운영체제 · python · 라이브러리 세 개, watch 카드와 같은 목록)

## golden  (04:39.7 ~ 06:08.3)

- **[04:39.7]** 여기까지는 노트북 안의 점검입니다. 번들만 읽는 서비스는 어떻게 확인할까요?  <sub>(6.9s)</sub>
  - 화면: 구간 제목 + 노트북 상자(점검 6개) | 서비스 상자(번들만) + 물음표 + 서비스용은 배포 번들 캡션
- **[04:46.6]** 답은 golden 사례입니다. 입력과 그 답을 짝지어 번들에 넣어 둔 기준 사례죠. 여기서 답은 실제 전력이 아니라, 학습 때 같은 모델로 계산해 둔 피처와 예측값입니다.  <sub>(13.3s)</sub>
  - 화면: 정의 카드 golden (비유 없음) + 아래 줄: 답 = 학습 때 계산해 둔 피처 · 예측 (실측 전력 아님)
- **[04:59.9]** 이걸로 하는 일이 자가검증입니다. 켜질 때마다 같은 계산을 다시 해서, 맞히는 실력이 아니라 학습 때와 똑같이 계산하는지를 보는 시험이죠.  <sub>(11.5s)</sub>
  - 화면: 정의 카드 selftest + 아래 줄: 맞히는 실력이 아니라 똑같이 계산하는지 보는 시험
- **[05:11.5]** golden 파일에는 입력 두 가지와 학습 때 값 두 가지가 있습니다. 입력은 21일치 기록 504행과 다음 날 계획 24행, 학습 때 값은 그날의 피처 24행 44칸과 예측 일곱 가지입니다.  <sub>(15.0s)</sub>
  - 화면: golden.json 네 칸: 원시 이력 504행(21일) · 계획 24행 [입력] · 기대 피처 24×44 · 기대 출력 7종 [학습 때 값]
- **[05:26.5]** 시험은 두 단계입니다. 피처를 다시 만들어 학습 때 피처와 맞춰 보고, 그 피처로 예측해 학습 때 예측과 맞춰 봅니다.  <sub>(9.5s)</sub>
  - 화면: 화살표 두 줄: 1단계 원시 이력 → 피처 만들기 → 기대 피처와 대조 / 2단계 기대 피처 → 번들 예측 → 기대 출력과 대조
- **[05:36.0]** 피처 44개 중 42개는 완전히 같아야 합니다. 최근 하루와 일주일 동안 전력이 얼마나 들쭉날쭉했는지 재는 피처 2개만 10억분의 1 차이를 허용하죠. 표 계산 도구 판다스의 아주 작은 누적 오차 때문이라고 코드에 적혀 있습니다.  <sub>(19.0s)</sub>
  - 화면: 코드 창 srv04_selftest.py, 1~5줄 띠 + 오른쪽 설명(1단계 · 피처 44개)
- **[05:55.0]** 예측은 운전 상태, 평균, 피크, 예측 범위 세 값이 완전히 같아야 하고, 확률만 1조분의 1까지 허용합니다. 운영체제마다 지수 계산이 조금 다를 수 있어서죠.  <sub>(13.3s)</sub>
  - 화면: 9~16줄 띠 + 설명 교체(2단계 · 예측 7종: 6종 완전 일치 + prob)

## replay  (06:08.3 ~ 07:11.8)

- **[06:08.3]** 마지막 증명은 리플레이입니다. 쉽게 말해 달력을 되돌려, 과거의 하루하루를 그날 밤 시점에서 다시 예측해 보는 겁니다.  <sub>(10.1s)</sub>
  - 화면: 구간 제목 + 정의 카드 replay
- **[06:18.4]** 9월 1일부터 14일까지, 날마다 그 전날 밤으로 돌아가, 전날까지의 기록과 그날의 생산 계획, 날씨만으로 그날 24시간을 예측합니다.  <sub>(11.6s)</sub>
  - 화면: 14일 달력 줄 — 예: 9월 5일 대상, 전력 기록은 전날까지 · 그날은 계획만 입력 · 이후 날짜는 쓰지 않음
- **[06:30.1]** 이번엔 노트북 없이, 서빙 코드가 원자료에서 피처부터 다시 만들어 예측합니다.  <sub>(6.6s)</sub>
  - 화면: 흐름: 원자료 → 서빙 코드(피처) → 평가 번들 → 336행
- **[06:36.7]** 제출 파일과 맞춰 보면, 평균과 피크 예측은 넷째 자리, 확률은 여섯째 자리까지, 그리고 경보 라벨까지 336행 모두 일치했습니다.  <sub>(10.9s)</sub>
  - 화면: 표 네 줄: 평균 예측(4자리) 336/336 · 피크 예측(4자리) · 경보 라벨 · 확률(6자리)
- **[06:47.6]** 평균 오차, 곧 예측이 실측과 평균 몇 킬로와트 어긋났는지를 재도, 리플레이 5.2199, 제출 파일 5.2199 킬로와트로 넷째 자리까지 같습니다.  <sub>(13.7s)</sub>
  - 화면: fact_row 두 줄: 리플레이 평균 오차(MAE) · 제출 파일 평균 오차(MAE)
- **[07:01.3]** 이력 창을 14일, 8일로 줄여 다시 돌려도 평균 예측 차이는 0 이었습니다. 8일은 서비스가 받아 주는 가장 짧은 이력이죠.  <sub>(10.5s)</sub>
  - 화면: 이력 창 세 막대: 전체(축척 생략 표시) / 14일 / 8일(최소) → 평균 예측 최대 차이 0.0

## watch  (07:11.8 ~ 07:45.1)

- **[07:11.8]** 짚고 갈 점입니다. 비트 단위로 같다는 건, 운영체제와 파이썬, 라이브러리 세 개의 버전까지 같은 환경에서의 이야기입니다. 다시 학습해 같은 파일이 나오는 것도 마찬가지죠.  <sub>(14.7s)</sub>
  - 화면: 짚고 갈 점 카드 1 — 환경(runtime 값: 운영체제 · python · lightgbm · numpy · pandas) 조건, 다시 학습 결과도 같은 조건
- **[07:26.5]** 버전이 다르면 서비스는 아예 켜지지 않고, 이 검사를 일부러 꺼도 golden 자가검증은 통과해야 켜집니다. 자세한 건 5편에서 봅니다.  <sub>(10.9s)</sub>
  - 화면: 카드 1 아래 한 줄: 라이브러리 버전이 다르면 서비스가 켜지지 않는다 · 검사를 꺼도 golden 자가검증은 통과해야
- **[07:37.4]** 또 제출 파일이 반올림돼 있어서, 두 번째 점검과 리플레이는 반올림한 자리까지의 비교입니다.  <sub>(7.7s)</sub>
  - 화면: 짚고 갈 점 카드 2 — 반올림 비교와 끝자리 비교

## outro  (07:45.1 ~ 08:19.4)

- **[07:45.1]** 정리합니다. 노트북은 번들을 저장하자마자 점검 여섯 개를 돌리고, 하나라도 실패하면 멈춥니다.  <sub>(8.4s)</sub>
  - 화면: 정리 제목 + 정리 1
- **[07:53.5]** 파일에서 다시 읽은 모델은 메모리의 모델과 비트 단위로 같았고, 다시 학습해도 모델 파일의 지문이 같았습니다.  <sub>(9.0s)</sub>
  - 화면: 정리 2
- **[08:02.5]** 서비스는 켜질 때마다 golden 사례로 자가검증을 하고, 리플레이는 제출 파일 336행을 반올림한 자리까지 똑같이 다시 만들었습니다.  <sub>(10.8s)</sub>
  - 화면: 정리 3·4
- **[08:13.3]** 다음 편에서는 서빙이 학습 때와 똑같은 코드로 피처를 만드는 방법을 봅니다.  <sub>(6.2s)</sub>
  - 화면: related_card 서빙 05 + series_map(2)
