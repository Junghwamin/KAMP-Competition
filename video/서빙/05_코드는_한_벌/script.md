# 코드는 한 벌 — 코드 생성과 검증하며 읽기

녹음용 대본 — 총 539.11초 (51컷)

> 괄호 안 숫자는 그 문장을 읽는 데 배정된 시간입니다. 크게 벗어나면 영상과 어긋납니다.

## intro  (00:00.0 ~ 01:07.6)

- **[00:00.0]** 여러분, 서빙은 학습 때와 똑같은 방법으로 피처, 곧 모델에 넣는 입력 44개를 만들어야 합니다. 그 코드를 학습 쪽과 어떻게 나눠 쓸까요?  <sub>(11.5s)</sub>
  - 화면: 왼쪽 '학습 · 노트북', 오른쪽 '서빙 · serving/' 상자 → 각각 '피처 44개' → 가운데 '= ?'
- **[00:11.5]** 과제를 짧게 짚으면, 전날 자정까지의 기록으로 다음 날 24시간의 시간별 전력과, 15분 피크가 187킬로와트 이상일 위험을 예측하는 일입니다.  <sub>(12.3s)</sub>
  - 화면: 과제 두 줄(칩 + 설명)
- **[00:23.9]** 오늘은 서빙 5편, 코드는 한 벌입니다.  <sub>(3.6s)</sub>
  - 화면: srv_title(5, …)
- **[00:27.4]** 오늘은 네 번째 칸, 피처 코드를 한 벌로 두고 번들을 검증하며 읽는 단계입니다.  <sub>(6.4s)</sub>
  - 화면: series_map(3) — '검증하며 읽기' 칸 밝힘 + 아래 복습 한 줄 '번들 = 서비스에 필요한 모델 파일 등을 한 폴더에 묶은 꾸러미'
- **[00:33.9]** 학습-서빙 불일치가 뭘까요? 한마디로, 학습 때와 실제 예측 때 피처가 조금이라도 다르게 만들어져, 모델이 엉뚱한 값을 내는 문제입니다.  <sub>(11.4s)</sub>
  - 화면: 정의 카드 skew (비유 딱지 포함)
- **[00:45.3]** 비유하자면 영점이 다른 두 저울입니다. 같은 물건에도 다른 숫자가 나오죠. 일반적으로 모델은 이럴 때 오류 없이 다른 답을 냅니다. 뒤에서 실험으로 봅니다.  <sub>(13.5s)</sub>
  - 화면: 카드의 비유 줄 강조
- **[00:58.8]** 우리 프로젝트는 이걸 두 겹으로 막습니다. 피처 코드는 한 벌만 두고, 번들을 읽을 때는 검문을 거칩니다.  <sub>(8.8s)</sub>
  - 화면: 두 칩: ① 코드는 한 벌 — 코드 생성 / ② 검증하며 읽기 — 로더

## why_not  (01:07.6 ~ 02:16.3)

- **[01:07.6]** 가장 쉬운 방법부터 떠올려 볼까요. 파이썬의 import 입니다. 한 줄만 적으면 다른 파일에 있는 함수를 빌려 올 수 있죠.  <sub>(9.8s)</sub>
  - 화면: 구간 제목 + 정의 카드 import
- **[01:17.4]** 우리 학습 코드는 src 폴더에, 노트북 장마다 파일 하나씩 있습니다. 그런데 이 파일들엔 함수 밖에 적혀서, 불러오자마자 실행되는 문장이 있습니다.  <sub>(12.1s)</sub>
  - 화면: 카드 퇴장 → 'import s02_features' 상자 + '피처 함수 몇 개만 쓰고 싶었는데…' + 아래 'src/ = 학습 코드 폴더 — 노트북의 장마다 파일 하나 (1장 = s01_diagnose.py …)'
- **[01:29.6]** 제2장 피처 파일을 import 하면, 제1장 파일이 원시 데이터를 읽고, 피처 파일은 전체 피처를 만들고 그림까지 저장합니다.  <sub>(9.9s)</sub>
  - 화면: 폭포처럼 세 줄: s01 최상위(함수 밖) df_raw = load_raw() · s02 최상위 feat = build_features(...) · 그림·표 저장
- **[01:39.5]** 번들 예측 함수가 든 제10장 파일은 더합니다. 모델을 학습하는 제5장을 포함해, 파일 10개가 줄줄이 따라 불려 옵니다.  <sub>(10.5s)</sub>
  - 화면: import s10_package → 회색 테두리로 묶은 칩 10개(s00~s09), s05 는 RIVAL '모델 학습' + '따라 불려 오는 파일 10개 (환경 설정 s00 + 1~9장)'
- **[01:50.0]** 그럼 필요한 함수만 손으로 복사해 오면 어떨까요? 비유하자면 같은 요리의 레시피를 두 장 베껴 둔 셈입니다.  <sub>(8.9s)</sub>
  - 화면: 비유 딱지 + 레시피 카드 두 장(학습 부엌 / 서빙 부엌), 둘 다 '소금 1스푼'
- **[01:58.9]** 그러다 보면 한쪽 레시피만 고치는 날이 오기 쉽습니다. 그날부터 두 부엌의 맛이 달라지죠.  <sub>(7.7s)</sub>
  - 화면: 학습 쪽 카드만 '소금 2스푼'(RIVAL)으로 바뀜
- **[02:06.6]** 우리 도구의 설명문은 이걸 드리프트라고 부릅니다. 복사본이 원본에서 멀어지는 것, 학습-서빙 불일치의 씨앗이죠.  <sub>(9.7s)</sub>
  - 화면: 짧은 정의 줄: 드리프트(drift) — 복사본이 원본에서 조금씩 멀어지는 것 + 출처 tools/build_serving_core.py 독스트링

## codegen  (02:16.3 ~ 03:44.9)

- **[02:16.3]** 그래서 택한 방법이 코드 생성, 프로그램이 코드를 직접 써 내는 겁니다. 비유하자면 원본 레시피에서 필요한 쪽만 복사기로 뽑는 거죠.  <sub>(11.0s)</sub>
  - 화면: 구간 제목 + 정의 카드 codegen (비유 딱지)
- **[02:27.3]** AST 는 코드를 문법 단위로 쪼갠 나무 구조입니다. 비유하자면 국어 시간의 문장 성분 분석이죠. 그래서 함수의 시작과 끝을 정확히 잘라냅니다.  <sub>(11.8s)</sub>
  - 화면: 정의 카드 ast (비유 딱지)
- **[02:39.1]** 도구는 src 파일 세 개에서 이름 20개의 원문을 뽑습니다. 제1장 6개, 제2장 12개, 제10장 2개죠.  <sub>(9.6s)</sub>
  - 화면: 왼쪽 src 상자 3개(개수) → 가운데 build_serving_core.py → 오른쪽 serving/_core.py
- **[02:48.7]** 결과는 484줄짜리 서빙 코어 파일. 라이브러리 불러오기와 함수·상수 정의뿐이라, import 해도 데이터를 읽거나 학습하지 않습니다.  <sub>(11.1s)</sub>
  - 화면: _core.py 상자 아래 '484줄 · 섹션 20개 · 함수·상수 정의만 — import 해도 데이터 적재·학습 없음'
- **[02:59.8]** 도구 코드 맨 위의 SPEC 목록은 어느 파일에서 어떤 이름을 뽑을지 적은 주문서입니다.  <sub>(6.7s)</sub>
  - 화면: 코드 창 srv05_codegen.py(왼쪽) + SPEC 줄 띠 + 오른쪽 설명
- **[03:06.6]** 아래 render 함수는 원문을 이어 붙이며, 조각마다 내용의 지문 앞 열두 자리를 머리줄에 적습니다.  <sub>(7.7s)</sub>
  - 화면: 띠가 render 로 이동, 설명 교체
- **[03:14.3]** 코어 파일엔 이런 머리줄이 20개 있고, 영상을 만들며 src 원문으로 다시 계산한 지문이 모두 같았습니다.  <sub>(8.6s)</sub>
  - 화면: 코드 퇴장 → 머리줄 3개(실제 파일·이름·지문) + '20 / 20 일치 ✔'
- **[03:22.8]** 안전장치는 세 겹. 뽑을 이름이 원본에 두 번 있거나, 생성 코드가 정의 안 된 이름을 쓰면 멈춥니다. 그런 이름은 없었죠. 끝으로 다시 만든 결과가 저장소 파일과 바이트까지 같은지 봅니다.  <sub>(15.7s)</sub>
  - 화면: 세 줄: ① 뽑을 이름이 두 번 정의 → 중단 ② 정의 안 된 이름 → 중단 (0개) ③ --check · 코드 생성 테스트 47개 가운데 하나
- **[03:38.5]** 이 확인은 코드 생성 테스트 47개 중 하나이고, 이 PC 에서도 통과했습니다.  <sub>(6.4s)</sub>
  - 화면: 실행 결과 한 줄 '[OK] serving/_core.py 가 src 와 일치한다'

## loader  (03:44.9 ~ 05:13.0)

- **[03:44.9]** 이제 두 번째 겹, 번들을 읽는 쪽입니다. 페일 패스트가 뭘까요? 이상한 게 보이면 일을 벌이기 전에 바로 멈추는 겁니다.  <sub>(10.3s)</sub>
  - 화면: 구간 제목 + 정의 카드 fail_fast (비유 딱지)
- **[03:55.2]** 비유하자면 비행 전 점검표죠. 한 항목이라도 걸리면 이륙하지 않습니다. 예측 창구인 우리 API 서버도 번들이 이상하면 켜지지 않습니다.  <sub>(11.5s)</sub>
  - 화면: 카드의 비유 줄 강조
- **[04:06.6]** 하나 더, 버전 고정. 남이 만든 코드 묶음인 라이브러리는 일반적으로 버전이 바뀌면 결과가 달라질 수 있어, 학습 때 버전으로 맞춰 둡니다.  <sub>(11.4s)</sub>
  - 화면: 정의 카드 pinning
- **[04:18.1]** 번들에는 학습 때의 라이트GBM 4.7.0, 넘파이 2.3.5, 판다스 3.0.5 가 적혀 있고, 로더가 지금 버전과 대조합니다.  <sub>(13.3s)</sub>
  - 화면: 카드 퇴장 → 버전 세 줄(매니페스트 runtime) ↔ 지금 버전 ✔ + '기본 설정에서는 하나라도 다르면 거부' + 두 줄(lightgbm 뜻 · 우회 스위치)
- **[04:31.3]** 번들을 읽는 함수는 코드 순서대로 일곱 계단을 오릅니다. 짐 목록인 매니페스트를 바이트로 읽고, 형식 버전과 테스트용 번들을 거르고, 버전을 맞추고, 매니페스트 자기 지문을 봅니다.  <sub>(15.1s)</sub>
  - 화면: 계단 도식 ①~④ 차례로 등장 + 왼쪽 위 설명 두 줄(계단 번호 = 코드 실행 순서 · FAST = 테스트용 번들)
- **[04:46.4]** 그다음 파일마다 지문을 맞춘 뒤에야 모델을 열어, 피처 이름과 순서, 레짐 수를 계약과 대조합니다. 끝으로 골든 사례로 피처를 다시 만들어 학습 때 값과 비교합니다. 켜질 때 학습-서빙 불일치를 한 번 더 잡는 거죠.  <sub>(17.2s)</sub>
  - 화면: 계단 ⑤~⑦ 등장 + 오른쪽 아래 golden 설명 세 줄(→ 다르면 SELFTEST_FEATURES)
- **[05:03.7]** 하나라도 어긋나면 예측기를 돌려주지 않습니다. 모두 오르는 데 이 PC 에서 146.2 밀리초가 걸렸죠.  <sub>(9.3s)</sub>
  - 화면: 계단 끝 '예측기' 상자(MINT) + 로드 시간 (이 PC 에서 잰 값)

## tamper  (05:13.0 ~ 06:33.8)

- **[05:13.0]** 코드로 보면 계단마다 모양이 같습니다. 조건을 보고, 어긋나면 오류 코드를 붙여 멈추죠. 첫 블록이 버전 대조입니다.  <sub>(10.3s)</sub>
  - 화면: 구간 제목 + 코드 창 srv05_loader.py(왼쪽) + 버전 블록 띠 + 설명
- **[05:23.3]** 다음 블록은 매니페스트의 자기 지문을 다시 계산해 적힌 값과 비교하고,  <sub>(5.5s)</sub>
  - 화면: 띠 이동 → 매니페스트 지문 블록
- **[05:28.8]** 그다음 파일마다 바이트를 읽어 지문을 대조하고, 모델은 그 바이트로 연 뒤 피처 이름과 순서까지 봅니다.  <sub>(8.4s)</sub>
  - 화면: 띠 이동 → 파일 지문 블록 → Booster·피처 이름 줄
- **[05:37.2]** 3편에서 본 무결성, 기억나시죠? 한마디로 파일이 처음 만든 모습 그대로라는 뜻입니다. 정말 지켜지는지 일부러 망가뜨려 보겠습니다.  <sub>(11.8s)</sub>
  - 화면: 코드 퇴장 → 정의 카드 integrity(복습)
- **[05:49.0]** 원본은 두고, 여섯 가지로 망가뜨려 로더 함수에 직접 넣어 봤습니다. 매니페스트가 없으면 첫 계단, 패스트 표시를 켜면 둘째, 버전 기록을 4.5.0 으로 바꾸면 셋째 계단에서 멈춥니다.  <sub>(16.2s)</sub>
  - 화면: 변조 실험 표(계단 순) 6행 등장 — 망가뜨린 것 · 걸린 계단 · 오류 코드 · 종료코드
- **[06:05.2]** 경보 기준 타우를 172.23 에서 160 으로 고치면 넷째 계단에서 걸립니다. 타우는 매니페스트 안에만 있어서, 파일 지문으로는 못 잡는 변조죠.  <sub>(13.1s)</sub>
  - 화면: τ 행 강조 사각형
- **[06:18.2]** 모델 파일 한 바이트를 바꾸거나 파일 하나를 지우면 다섯째 계단입니다. 여섯 경우 모두 번들 오류로 멈췄죠. 명령줄 프로그램이라면 끝날 때 돌려주는 번호, 종료코드 3 을 받는 오류입니다.  <sub>(15.6s)</sub>
  - 화면: 강조 사각형이 ⑤ 두 행(종료코드 열 앞까지)으로 이동 → '종료코드 (CLI 라면)' 열 강조

## names  (06:33.8 ~ 07:57.2)

- **[06:33.8]** 그런데 왜 이름만이 아니라 순서까지 약속할까요? 이름표 없이 숫자만 줄 세워 넣으면, 모델은 몇 번째 칸인지 자리로만 읽습니다.  <sub>(10.6s)</sub>
  - 화면: 구간 제목 + 44칸 줄(첫 칸·셋째 칸 이름표)
- **[06:44.5]** 실험해 봤습니다. 공장이 한창 돌아가는 가동 레짐의 평균전력 모델 하나에, 어제와 일주일 전 같은 시각 전력, 이 두 칸을 바꿔 넣었습니다.  <sub>(11.4s)</sub>
  - 화면: 두 칸이 자리를 바꿈(Swap)
- **[06:55.9]** 오류는 없었습니다. 그런데 336행 중 317행의 예측이 달라졌고, 최대 23.95킬로와트, 평균 3.21킬로와트가 어긋났습니다.  <sub>(13.2s)</sub>
  - 화면: 결과 세 줄: 오류 없음 / 317 / 336행 / 최대 23.95 kW · 평균 3.21 kW
- **[07:09.1]** 그래서 번들 예측 함수는 예측 직전에 이름을 보고 계약 순서로 다시 세웁니다. 칸을 통째로 거꾸로 넣어도 평균전력 예측이 같았죠. 줄 끝 플로트 64 는 바로 다음에 봅니다.  <sub>(14.1s)</sub>
  - 화면: 코드 한 줄(원문 인용) Xf = X.reindex(columns=cols).astype(np.float64) + '칸을 거꾸로 넣어도 평균전력 예측이 같다 ✔' + 줄 끝 float64 에 '← 바로 다음' 표시
- **[07:23.2]** 플로트 64 가 뭘까요? 컴퓨터가 소수를 적는 칸의 크기입니다. 64비트 칸은 32비트 칸보다 훨씬 긴 자릿수를 담죠. 비유하자면 넓은 공책과 좁은 공책입니다.  <sub>(14.2s)</sub>
  - 화면: 정의 카드 float (비유 딱지)
- **[07:37.4]** 입력을 플로트 32 로 한 번 내렸다 되돌리기만 해도, 336행 중 92행의 예측이 바뀌었고 최대 13.39킬로와트가 달라졌습니다.  <sub>(11.8s)</sub>
  - 화면: 카드 퇴장 → 결과 두 줄(92 / 336행 · 최대 13.39 kW)
- **[07:49.2]** 일반적으로, 트리의 갈림 기준 바로 옆에 있던 값은 반올림만으로도 다른 가지로 넘어갈 수 있습니다.  <sub>(8.1s)</sub>
  - 화면: 작은 갈림 그림(일반론 표시) 등장 → 2초 뒤, 나레이션 도중 기준 근처 점이 반대쪽 가지로 넘어감

## watch  (07:57.2 ~ 08:21.8)

- **[07:57.2]** 짚고 갈 점 하나. 라이트GBM 에서 모델 파일을 경로로 여는 방법은, 한글이 든 경로를 열지 못합니다. 이 PC 에서 저장소를 둔 폴더 이름에도 한글이 있죠.  <sub>(12.4s)</sub>
  - 화면: 구간 제목 + 짚고 갈 점 카드
- **[08:09.7]** 그래서 로더는 파일을 바이트로 한 번 읽어 지문 확인에 쓰고, 바로 그 바이트로 모델을 엽니다. 덕분에 검사와 열기 사이에 파일이 바뀔 틈도 없습니다.  <sub>(12.1s)</sub>
  - 화면: 카드 아래 흐름: 파일 → 바이트 b → sha256(b) 대조 / Booster(model_str=b) — 같은 b

## outro  (08:21.8 ~ 08:59.1)

- **[08:21.8]** 정리합니다. 학습과 서빙의 피처 코드는 한 벌이고, 서빙 코어 파일은 src 원문에서 자동으로 만듭니다.  <sub>(8.6s)</sub>
  - 화면: 정리 1
- **[08:30.4]** AST 로 이름 20개의 원문을 뽑아 조각마다 지문을 남기고, 테스트가 동기화를 강제합니다.  <sub>(7.5s)</sub>
  - 화면: 정리 2
- **[08:37.8]** 번들은 일곱 계단의 검문을 모두 통과해야 예측기가 되고, 하나라도 어긋나면 멈춥니다.  <sub>(7.2s)</sub>
  - 화면: 정리 3
- **[08:45.0]** 모델은 자리로 읽기 때문에, 피처는 이름과 순서, 그리고 플로트 64 까지 약속합니다.  <sub>(7.3s)</sub>
  - 화면: 정리 4
- **[08:52.3]** 다음 편에서는 9월 13일 월요일, 하루치 예측이 만들어지는 길을 따라갑니다.  <sub>(6.8s)</sub>
  - 화면: 관련 편 카드 + series_map
