# 하루치 예측이 만들어지는 길

녹음용 대본 — 총 571.83초 (55컷)

> 괄호 안 숫자는 그 문장을 읽는 데 배정된 시간입니다. 크게 벗어나면 영상과 어긋납니다.

## intro  (00:00.0 ~ 00:54.9)

- **[00:00.0]** 9월 12일 일요일, 밤 12시입니다. 무엇을 넣으면 월요일 하루 24시간의 예측이 나올까요?  <sub>(8.6s)</sub>
  - 화면: 날짜·시각 표시(9월 12일 일 24:00) + 질문 문장
- **[00:08.6]** 이 과제는 전날 자정까지의 기록으로 다음 날 24시간의 시간별 평균전력을 예측하고, 15분 피크가 187킬로와트 이상인 위험한 시간을 골라내는 것입니다.  <sub>(13.0s)</sub>
  - 화면: 과제 A(회귀)·B(판정) 칩 두 줄, θ 는 회색
- **[00:21.6]** 숫자 값을 맞히는 모델을 회귀, 여러 종류 가운데 하나를 고르는 모델을 분류라고 합니다.  <sub>(7.2s)</sub>
  - 화면: 과제 칩 아래 두 줄 정의: 회귀 모델 = 숫자 값을 맞힌다 (예: 평균전력 kW) · 분류 모델 = 여러 종류 가운데 하나를 고른다
- **[00:28.8]** 서빙 여섯 번째 편, 하루치 예측이 만들어지는 길입니다.  <sub>(4.8s)</sub>
  - 화면: srv_title(6)
- **[00:33.6]** 지금까지 모델을 한 폴더의 번들로 저장하고, 같은 모델임을 증명하고, 검증하며 읽었습니다. 이제 그 번들로 하루치 예측을 만듭니다.  <sub>(11.2s)</sub>
  - 화면: series_map(4) — '하루치 예측' 칸 강조
- **[00:44.8]** 9월 13일 월요일 하루를 따라가 보죠. 입력이 검문과 정비를 지나 피처가 되고, 게이트와 회귀를 거쳐 출력이 됩니다.  <sub>(10.1s)</sub>
  - 화면: 이 편의 길: 입력 → 검문 → 정비 → 피처 → 게이트·회귀 → 출력 (pipeline 6칸)

## inputs  (00:54.9 ~ 02:02.3)

- **[00:54.9]** 들어가는 입력은 두 가지뿐입니다. 지나간 기록인 이력, 그리고 내일의 계획이죠.  <sub>(7.2s)</sub>
  - 화면: 구간 제목 + 상자 두 개(이력 · 계획) → 하루치 예측
- **[01:02.1]** 스키마가 뭘까요? 데이터의 모양에 대한 규칙입니다. 어떤 칸이 어떤 형식과 범위로 있어야 하는지 정해 둔 것이죠.  <sub>(9.8s)</sub>
  - 화면: 정의 카드: 스키마
- **[01:11.9]** 이력은 14칸, 날짜와 시간, 전력 다섯 칸, 생산량과 기상입니다. 전날 23시에 끝나는 빈틈없는 시간별 기록이어야 하죠.  <sub>(10.8s)</sub>
  - 화면: 이력 14칸 칩(전력 5칸 노랑) + 규칙 한 줄
- **[01:22.7]** 길이는 최소 8일, 권장 14일입니다. 다른 프로그램이 API로 보낼 때만 최대 62일까지 받습니다.  <sub>(8.7s)</sub>
  - 화면: 길이 규칙(회색 칩: 최소 8일 · 권장 14일 · 최대 62일 (API))
- **[01:31.4]** 계획은 8칸, 대상일 0시부터 23시까지 24행입니다. 생산량은 생산관리 시스템인 ERP의 계획, 기상은 예보이고, 전력 칸은 없습니다.  <sub>(12.0s)</sub>
  - 화면: 계획 8칸 칩 + 'ERP(생산관리 시스템)' + '전력 칸 없음'
- **[01:43.4]** 이번 추적은 지난날을 다시 돌리는 리플레이라서, 계획 자리에 그날의 실제 기록을 넣었습니다.  <sub>(7.7s)</sub>
  - 화면: 9/13 계획 3행 표(0·7·14시: 생산량·기온) + 리플레이 딱지·캡션(원자료의 그날 값)
- **[01:51.1]** 9월 13일은 0시 생산량이 0, 7시 196, 14시 1018입니다. 공장이 아침에 깨어나는 게 계획에 이미 담겨 있죠.  <sub>(11.2s)</sub>
  - 화면: 생산량 7시·14시 값 민트로 + '아침에 공장이 깨어난다'

## leakage  (02:02.3 ~ 03:07.7)

- **[02:02.3]** 예측 전에 검문부터 합니다. 입력 검증, 들어온 데이터가 규칙을 지키는지 확인하는 일이죠. 어긋나면 예측하지 않고 오류 코드로 이유를 알려 줍니다.  <sub>(12.6s)</sub>
  - 화면: 구간 제목 + 정의 카드: 입력 검증
- **[02:14.9]** 가장 중요한 검문은 데이터 누수입니다. 예측하는 순간에는 알 수 없는 미래 값이 입력에 섞이는 것이죠. 비유하자면 시험지에 정답이 미리 적혀 있는 셈입니다.  <sub>(13.1s)</sub>
  - 화면: 정의 카드: 데이터 누수 (+비유 딱지)
- **[02:28.0]** 그래서 계획에 대상일 전력, 예를 들어 평균 칸이 들어 있으면 '계획에 전력이 있다'는 오류로 거부합니다. 내일 전력을 이미 안다는 뜻이니까요.  <sub>(11.5s)</sub>
  - 화면: 계획 칩 줄 + 끼어든 '평균' 칩(빨강) ✕ → PLAN_HAS_POWER
- **[02:39.5]** 다른 검문도 실제로 돌려 봤습니다. 이력이 5일뿐이면 9월 6일부터 보내라고 알려 주고, 시각이 빠지거나, 계획이 23행이거나, 공휴일표 밖의 날짜여도 거부합니다.  <sub>(14.3s)</sub>
  - 화면: 입력 오류 표 5행(입력 · 오류 코드 · 종료코드) — SV input_errors
- **[02:53.8]** 종료코드는 프로그램이 끝날 때 남기는 번호로, 0은 성공, 2는 입력 오류입니다. 다섯 경우 모두 2였죠. 숫자를 조용히 내지 않고, 멈춰서 이유를 알려 줍니다.  <sub>(13.8s)</sub>
  - 화면: 종료코드 열 강조(2) + 정의 두 줄(종료코드 = 프로그램이 끝날 때 남기는 번호 · 0 성공 · 2 입력 오류)

## causal  (03:07.7 ~ 04:30.8)

- **[03:07.7]** 인과성이란, 예측하는 순간에 이미 알 수 있는 정보만 쓰는 성질입니다. 그래서 전날 23시까지의 기록과, 미리 정한 다음 날 계획만 씁니다.  <sub>(11.7s)</sub>
  - 화면: 구간 제목 + 정의 카드: 인과성
- **[03:19.4]** 보간은 비어 있는 값을 앞뒤 값으로 메우는 것입니다. 우리 데이터에는 평균전력이 0으로 찍힌 계측정지 구간이 있어서, 이런 칸을 이력 안에서만 메웁니다.  <sub>(12.7s)</sub>
  - 화면: 정의 카드: 보간
- **[03:32.1]** 만약 계획 행까지 이력과 함께 메우면 어떻게 될까요?  <sub>(4.4s)</sub>
  - 화면: 칸 줄: 9/12 20~23시 평균(이력) | 9/13 0~3시 '?'(대상일 전력 칸)
- **[03:36.4]** 이 보간은 맨 끝 빈칸도 가장 가까운 값으로 채웁니다. 그래서 대상일 전력 칸이 모두 전날 23시 값, 23킬로와트가 되죠. 가짜 실측입니다.  <sub>(12.1s)</sub>
  - 화면: '?' → 23 으로 채워짐(빨강) + '가짜 실측 — 끝의 빈칸까지 가장 가까운 값으로' + 근거 _core.py limit_direction='both'
- **[03:48.6]** 그래서 코드는 순서를 고정합니다. 첫째, 정비는 이력에만 합니다.  <sub>(6.1s)</sub>
  - 화면: 코드 창 srv06_causal.py(문자열 밝게 다시 칠함) + 띠 1~2줄, 설명 ①
- **[03:54.6]** 둘째, 계획 행의 전력 칸은 '값 없음'으로 비워 둡니다.  <sub>(4.4s)</sub>
  - 화면: 띠 4~14줄, 설명 ②
- **[03:59.1]** 셋째, 둘을 이어 붙입니다. 계획에 풍속이나 인원이 비어 있으면 이 단계에서 메우고, 전력 칸은 빈 채로 둡니다.  <sub>(9.4s)</sub>
  - 화면: 띠 16~20줄, 설명 ③
- **[04:08.5]** 넷째, 날마다 생산량 합이 0인 날을 휴무일로 표시한 달력, 가동 캘린더를 만듭니다.  <sub>(7.1s)</sub>
  - 화면: 띠 22줄, 설명 ④
- **[04:15.6]** 다섯째, 피처를 만듭니다. 지난 하루와 일주일의 피크 횟수를 세는 칸에는, 번들 설명서인 매니페스트에 적힌 세타 187을 그대로 씁니다.  <sub>(11.8s)</sub>
  - 화면: 띠 24줄, 설명 ⑤
- **[04:27.4]** 여섯째, 대상일 24행만 남깁니다.  <sub>(3.5s)</sub>
  - 화면: 띠 25줄, 설명 ⑥

## features  (04:30.8 ~ 05:03.9)

- **[04:30.8]** 그 결과가 24행 곱하기 44칸의 피처입니다. 피처는 모델에 넣는 입력 한 칸이죠. 9월 13일의 세 시각만 뽑아 보겠습니다.  <sub>(11.1s)</sub>
  - 화면: 구간 제목 + '24 × 44' 격자 → 피처 표(8행 × 3시각)
- **[04:41.9]** 요일 칸은 모두 0, 월요일이라는 뜻입니다. 전날 휴무 칸은 1로 켜져 있고, 그날 휴무 칸은 0입니다.  <sub>(8.8s)</sub>
  - 화면: dow·전날 휴무·그날 휴무 행 강조
- **[04:50.8]** 어제 같은 시각 전력은 23, 23, 25킬로와트로 낮습니다. 어제가 일요일 휴무였으니까요. 반면 지난주 월요일 14시는 160이었습니다.  <sub>(13.1s)</sub>
  - 화면: lag24 행(빨강) vs lag168 행(민트) 강조

## gate  (05:03.9 ~ 06:22.6)

- **[05:03.9]** 이제 게이트입니다. 레짐은 공장의 운전 상태, 게이트는 예측할 시각이 어느 레짐일지 먼저 고르는 분류 모델입니다. 비유하자면 접수처가 진료과를 정해 주는 것이죠.  <sub>(13.9s)</sub>
  - 화면: 구간 제목 + 정의 카드: 게이트·레짐 (+비유 딱지)
- **[05:17.8]** 레짐은 그 시각의 평균전력으로 나눕니다. 30킬로와트 미만은 레짐 0, 70 미만은 레짐 1, 그 이상은 레짐 2입니다.  <sub>(10.5s)</sub>
  - 화면: 평균전력 수직선: 30·70 kW 경계(회색 점선, 연구자 기준) + 구간 세 칸(레짐 색)
- **[05:28.3]** 9월 13일은 0시부터 6시까지 레짐 0, 7시 한 시간은 레짐 1, 8시부터는 레짐 2를 골랐습니다.  <sub>(8.9s)</sub>
  - 화면: 24칸 띠(레짐 색) + 시각 눈금
- **[05:37.2]** 7시는 아슬아슬했습니다. 레짐 1이 0.601, 레짐 2가 0.399로, 큰 쪽인 레짐 1을 골랐죠. 실제 7시 평균도 63킬로와트, 레짐 1 구간이었습니다.  <sub>(15.5s)</sub>
  - 화면: 7시 칸 강조 + 게이트 확률 막대 3개 + 실측 63 kW → 레짐 1 구간
- **[05:52.6]** 레짐마다 전용 회귀 모델이 있습니다. 새벽 1시, 레짐 0 모델은 22.36킬로와트를 냈고, 레짐 2 모델이었다면 97.85킬로와트였을 겁니다. 실제는 23킬로와트였죠.  <sub>(16.5s)</sub>
  - 화면: 1시: 레짐 0 모델 22.36 ✔ / 레짐 2 모델 97.85(만약) / 실측 23
- **[06:09.1]** 학습 때 보지 못한 9월 13일 하루, 예측과 실측 차이의 평균인 평균 절대 오차, MAE는 2.90킬로와트로, 2주 전체의 5.2199보다 작습니다.  <sub>(13.4s)</sub>
  - 화면: 24시간 막대(예측, 레짐 색) + 실측 점 + MAE 두 줄

## outputs  (06:22.6 ~ 07:51.6)

- **[06:22.6]** 출력은 한 시간에 13칸입니다. 9월 13일 9시 행으로 읽어 보죠.  <sub>(6.3s)</sub>
  - 화면: 구간 제목 + 출력 표(컬럼 · 뜻 · 9시 값) 12행 + ts
- **[06:28.9]** 피크 경보는 피크 예측 186.45킬로와트가 경보 기준 타우 172.23 이상이라 1입니다.  <sub>(9.2s)</sub>
  - 화면: peak_label 행 + y_peak_pred 행 강조, τ 회색
- **[06:38.1]** 세타 187보다 낮죠? 회귀 예측은 평균 쪽으로 쏠려서, 과거 검증에서 경보 성적이 가장 좋았던 값들로 기준을 정했습니다.  <sub>(10.4s)</sub>
  - 화면: 옆 메모 'τ < θ 187 · 검증 조각 4개에서 각각 경보 성적(F1)이 최대인 값들의 중앙값'
- **[06:48.5]** 확률 보정은, 모델이 낸 확률을 실제로 일어난 비율에 맞게 고치는 것입니다. 그 대응표가 번들에 JSON 텍스트 파일로 들어 있죠.  <sub>(10.4s)</sub>
  - 화면: 정의 카드: 확률 보정
- **[06:58.9]** 피크 확률은 게이트와 별개인 피크 분류 모델이 냅니다. 9시의 0.843이 보정을 거쳐 0.551이 되죠. 확률 경보는 보정 전 값이 0.01 이상이면 1입니다.  <sub>(15.5s)</sub>
  - 화면: 표 복귀, peak_prob → peak_prob_cal 행(별도 피크 분류 모델), peak_label_cls 행(보정 전 확률 ≥ τ_cls) 강조
- **[07:14.4]** 예측구간은 예측값 위아래로, 실제값이 정해 둔 비율만큼 들어오도록 붙인 범위입니다.  <sub>(7.2s)</sub>
  - 화면: 정의 카드: 예측구간
- **[07:21.5]** 폭 21.6은, 과거 검증에서 예측이 빗나간 크기의 90퍼센트가 들어온 폭입니다. 휴무일은 빗나감이 커서 82.6이죠.  <sub>(11.6s)</sub>
  - 화면: 표 복귀, pi_lo·pi_hi 행 강조 + 옆 메모(폭 = 검증 오차의 90% 크기 · 운영일/휴무일)
- **[07:33.2]** 그래서 9시 예측 182.34킬로와트의 구간은 160.74에서 203.94입니다.  <sub>(9.4s)</sub>
  - 화면: y_avg_pred 값·pi 값 민트로 이어짐 + q10·q50·q90 행 회색 상자(→ 짚고 갈 점 ②)
- **[07:42.6]** 9월 13일 하루로 보면, 실제 피크 5시간을 경보 8시간으로 모두 잡았고, 오경보가 3시간이었습니다.  <sub>(9.0s)</sub>
  - 화면: 24칸 띠 두 줄(실제 피크 / 경보) + 적중 5 · 오경보 3 · 놓침 0

## watch  (07:51.6 ~ 08:54.4)

- **[07:51.6]** 짚고 갈 점 셋입니다. 첫째, 확률 경보 기준은 왜 딱 0.01일까요? 후보를 소수 둘째 자리까지만 만들어 0.01 간격으로만 고를 수 있었고, 그중 경보 성적이 가장 좋은 값이 0.01이었습니다.  <sub>(17.9s)</sub>
  - 화면: 구간 제목 + 짚고 갈 점 카드 ①
- **[08:09.5]** 둘째, 분위 예측 세 칸입니다. 실제값이 그보다 낮을 확률이 10, 50, 90퍼센트가 되도록 따로 학습한 값이죠.  <sub>(10.2s)</sub>
  - 화면: 짚고 갈 점 카드 ② + 첫 줄(분위 예측 뜻) 강조 띠
- **[08:19.7]** 구간 안에 실제값이 들어온 비율을 피복률이라 합니다. 예측구간은 테스트 2주 동안 0.9911로, 목표 0.9를 넘었습니다.  <sub>(12.0s)</sub>
  - 화면: 강조 띠 → 피복률·예측구간 줄
- **[08:31.7]** 반면 분위 예측의 10에서 90퍼센트 사이는 0.497로, 목표 0.8에 못 미칩니다. 비율을 맞추는 보정을 안 해서 참고용이죠.  <sub>(12.3s)</sub>
  - 화면: 강조 띠 → 분위 예측 피복률 줄(미보정)
- **[08:44.0]** 셋째, 학습 때 기상은 예보 대신 실제 관측값을 썼습니다. 운영에서 예보를 넣으면 예보가 틀린 만큼 성능이 떨어질 수 있습니다.  <sub>(10.4s)</sub>
  - 화면: 짚고 갈 점 카드 ③

## outro  (08:54.4 ~ 09:31.8)

- **[08:54.4]** 정리하겠습니다. 입력은 이력과 계획 두 가지, 스키마와 누수 검문부터 통과합니다. 정비는 이력에만 해서 대상일 전력 칸은 비워 둡니다.  <sub>(11.7s)</sub>
  - 화면: 정리 1·2
- **[09:06.1]** 피처 44칸을 보고 게이트가 레짐을 고르면, 그 레짐 전용 회귀 모델이 평균전력과 피크를 예측합니다. 피크 확률은 별도의 분류 모델이 냅니다.  <sub>(11.8s)</sub>
  - 화면: 정리 3
- **[09:17.9]** 출력 13칸에는 경보 두 가지와 보정 확률, 예측구간이 담깁니다.  <sub>(5.6s)</sub>
  - 화면: 정리 4
- **[09:23.6]** 다음 편에서는 이 예측을 명령어로 쓰는 CLI와, 프로그램끼리 쓰는 API로 내보내고 운영하는 법을 봅니다.  <sub>(8.2s)</sub>
  - 화면: 관련 편 카드 + series_map(4)
