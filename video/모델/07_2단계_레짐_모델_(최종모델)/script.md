# 2단계 레짐 모델 (최종모델)

녹음용 대본 — 총 481.51초 (47컷)

> 괄호 안 숫자는 그 문장을 읽는 데 배정된 시간입니다. 크게 벗어나면 영상과 어긋납니다.

## intro  (00:00.0 ~ 00:46.5)

- **[00:00.0]** 여러분, 이 공장의 전력 곡선을 한번 보세요.  <sub>(3.9s)</sub>
  - 화면: F01 전체기간 시계열 그림이 들어온다
- **[00:03.9]** 평균전력이 30킬로와트도 안 되는 시간이 전체의 33.2퍼센트이고, 가장 높을 때는 222킬로와트까지 올라갑니다.  <sub>(10.7s)</sub>
  - 화면: 그림 오른쪽에 두 값 라벨(33.2%, 222 kW)
- **[00:14.6]** 이렇게 성격이 전혀 다른 두 세계를 모델 하나로 한꺼번에 맞히려 하면, 어떤 일이 생길까요?  <sub>(7.3s)</sub>
  - 화면: 그림이 흐려지고 질문 문장
- **[00:21.9]** 오늘은 이 프로젝트의 최종모델, 2단계 레짐 모델을 뜯어보겠습니다.  <sub>(6.0s)</sub>
  - 화면: 강의 제목 카드
- **[00:27.9]** 과제부터 짧게 짚고 가죠. 전날 자정까지의 데이터만 보고 다음 날 24시간의 시간별 평균전력을 예측하는 것이 첫 번째 과제이고,  <sub>(10.4s)</sub>
  - 화면: Task A 칩: 다음 날 24시간 평균전력 예측(회귀)
- **[00:38.2]** 그 예측으로 15분 피크가 187킬로와트를 넘을 위험한 시간을 골라내는 것이 두 번째 과제입니다.  <sub>(8.3s)</sub>
  - 화면: Task B 칩: peak15 ≥ θ 판정

## regimes  (00:46.5 ~ 01:33.5)

- **[00:46.5]** 먼저 레짐, 우리말로 운전 상태라는 말부터 정의하겠습니다.  <sub>(5.2s)</sub>
  - 화면: 구간 제목 '레짐이란'
- **[00:51.7]** 하루 평균전력으로 날짜를 나눠 보면 세 무리로 갈라집니다. 거의 멈춘 날 49일, 반쯤 도는 날 38일, 제대로 가동한 날 170일입니다.  <sub>(11.8s)</sub>
  - 화면: 세 막대(49/38/170일) + 레짐별 평균전력 22.11/46.08/124.64
- **[01:03.5]** 코드는 이 경계를 두 개의 문턱으로 적습니다. 30킬로와트 아래는 기저, 30에서 70 사이는 중간, 70 이상은 가동입니다.  <sub>(11.1s)</sub>
  - 화면: 수직선 0~150 kW, 30과 70에 회색 점선, 세 구역 색칠
- **[01:14.6]** 2분류 모델은 70 하나만 씁니다. 가동이냐 아니냐, 둘로만 나누는 셈이죠.  <sub>(7.0s)</sub>
  - 화면: 30 점선이 흐려지고 두 구역만 남음
- **[01:21.6]** 여기서 30과 70은 데이터에서 계산된 값이 아닙니다. 탐색적 분석을 보고 연구자가 정한 경계라서, 화면에는 회색 점선으로 표시했습니다.  <sub>(11.9s)</sub>
  - 화면: 점선 라벨 '연구자가 정한 기준'

## arch  (01:33.5 ~ 02:34.1)

- **[01:33.5]** 이제 구조를 보겠습니다. 이름 그대로 두 단계로 되어 있습니다.  <sub>(5.7s)</sub>
  - 화면: 왼쪽 입력 상자 '44개 피처'
- **[01:39.2]** 첫 번째 단계는 게이트입니다. 44개 피처를 받아서, 이 시간이 세 레짐 가운데 어디에 속하는지를 분류합니다. 400그루짜리 LightGBM 분류기입니다.  <sub>(13.1s)</sub>
  - 화면: 게이트 상자(LGBMClassifier · 400트리 · multiclass)
- **[01:52.2]** 두 번째 단계는 레짐마다 따로 학습한 회귀 모델입니다. 기저 전용, 중간 전용, 가동 전용이 있고, 모두 800그루짜리 LightGBM입니다.  <sub>(11.5s)</sub>
  - 화면: 오른쪽에 회귀 상자 3개가 세로로
- **[02:03.7]** 이 회귀 모델은 시간별 평균전력용과 15분 피크용으로 한 벌씩, 모두 두 벌입니다.  <sub>(6.9s)</sub>
  - 화면: 회귀 상자 3개가 두 겹(y_avg / y_peak)으로 겹쳐 보임
- **[02:10.6]** 마지막으로 안전장치가 하나 있습니다. 학습 데이터 전체로 맞춘 fallback 회귀인데, 학습 표본이 20개도 안 되는 레짐으로 배정된 시간은 이 모델이 대신 맡습니다.  <sub>(13.3s)</sub>
  - 화면: 아래에 fallback 상자(회색)
- **[02:23.9]** 세어 보면 게이트 1개, 레짐 회귀 최대 6개, fallback 2개. 3분류 모델 하나 안에 LightGBM이 최대 아홉 개 들어 있습니다.  <sub>(10.2s)</sub>
  - 화면: 1 + 6 + 2 = 9 합계 줄

## gating  (02:34.1 ~ 03:45.2)

- **[02:34.1]** 그런데 게이트의 판단을 어떻게 쓰느냐에 따라 답이 크게 달라집니다. 예시 숫자로 확인해 보죠.  <sub>(8.0s)</sub>
  - 화면: 구간 제목 '하드 게이팅'
- **[02:42.1]** 어떤 한 시간에 게이트가 기저 0.05, 중간 0.15, 가동 0.80의 확률을 냈다고 해 보겠습니다.  <sub>(10.1s)</sub>
  - 화면: 확률 막대 3개(예시 값)
- **[02:52.2]** 세 회귀 모델은 각각 18, 52, 164킬로와트를 예측했다고 하죠.  <sub>(6.3s)</sub>
  - 화면: 각 막대 아래 회귀 출력 값
- **[02:58.5]** 우리 코드는 가장 큰 확률 하나만 고릅니다. 가동이 0.80으로 가장 크니, 답은 가동 회귀의 164킬로와트입니다. 이것을 하드 게이팅이라고 부릅니다.  <sub>(13.9s)</sub>
  - 화면: 가동 막대 강조, 164 kW 로 화살표
- **[03:12.4]** 만약 확률로 가중평균을 냈다면 0.05 곱하기 18, 0.15 곱하기 52, 0.80 곱하기 164를 더해서 139.9킬로와트가 됩니다.  <sub>(14.2s)</sub>
  - 화면: 검산 스택: 세 곱과 합 139.9
- **[03:26.6]** 가동 중인 시간인데, 다른 레짐의 답이 섞여 들어와 24킬로와트 넘게 끌어내려진 겁니다.  <sub>(7.5s)</sub>
  - 화면: 164 와 139.9 사이 간격 표시
- **[03:34.0]** 하드 게이팅은 이런 섞임을 막아 줍니다. 대신 게이트가 레짐을 잘못 고르면, 엉뚱한 전용 모델의 답이 그대로 나온다는 약점이 있습니다.  <sub>(11.2s)</sub>
  - 화면: 장단점 두 줄(✔ / ✕)

## code  (03:45.2 ~ 04:42.2)

- **[03:45.2]** 이제 실제 코드를 보겠습니다. s05 모델 파일의 make_regime_model 함수입니다.  <sub>(6.8s)</sub>
  - 화면: 코드 창(regime_fit.py) + 파일 위치 태그
- **[03:52.0]** 맨 위 regime_label이 방금 본 문턱입니다. 2분류면 70 이상인지만 보고, 3분류면 넘파이 digitize로 30과 70을 기준 삼아 0, 1, 2를 매깁니다.  <sub>(12.8s)</sub>
  - 화면: 1~4줄 띠
- **[04:04.8]** 다음 블록이 게이트입니다. 3분류면 multiclass, 2분류면 binary를 목적함수로 씁니다.  <sub>(7.6s)</sub>
  - 화면: 10~14줄 띠
- **[04:12.5]** 그 아래 반복문이 레짐별 회귀입니다. 표본이 20개 미만인 레짐은 continue로 건너뛰고, 반복이 끝나면 전체 데이터로 fallback을 하나 더 맞춥니다.  <sub>(12.0s)</sub>
  - 화면: 19~30줄 띠
- **[04:24.5]** 예측 함수를 보면 gate.predict로 레짐 번호 하나를 받고, 그 번호의 회귀 모델만 부릅니다. 확률을 섞지 않는다는 것이 코드에서도 확인되죠.  <sub>(11.9s)</sub>
  - 화면: 코드 창 교체(regime_predict.py), 2줄과 5~8줄 띠
- **[04:36.4]** 어느 레짐에도 배정받지 못한 행은 마지막 두 줄에서 fallback이 채웁니다.  <sub>(5.8s)</sub>
  - 화면: 10~11줄 띠

## results  (04:42.2 ~ 06:05.0)

- **[04:42.2]** 그러면 성적표를 보겠습니다. 여기 수치는 모두 KAMP 포드에서 FULL 모드로 돌린 실행본에서 가져왔습니다.  <sub>(8.9s)</sub>
  - 화면: 출처 캡션 + 표 머리
- **[04:51.1]** 교차검증 OOF MAE는 3분류 레짐 모델이 11.007로 가장 낮고, 2분류가 11.670, 단일 LightGBM이 15.355입니다.  <sub>(12.3s)</sub>
  - 화면: 표 5행(OOF MAE, 테스트 MAE) — 레짐 행 강조
- **[05:03.5]** 테스트 2주, 336시간에서도 5.220으로 가장 낮습니다. 가이드북 계열에서 가장 좋았던 랜덤 포레스트의 6.039보다 13.56퍼센트 낮은 값입니다.  <sub>(15.5s)</sub>
  - 화면: 테스트 MAE 열 강조, 개선율 줄
- **[05:19.0]** 그런데 이 모델의 진짜 강점은 평균이 아니라 한 구간에서 드러납니다. 하계휴가와 재가동이 들어 있는 폴드 4입니다.  <sub>(9.7s)</sub>
  - 화면: fold별 MAE 막대(4개 fold × 레짐3·Naive)
- **[05:28.7]** 일주일 전 값을 그대로 쓰는 Seasonal Naive는 이 구간에서 MAE가 69.292까지 무너지지만, 레짐 모델은 7.161입니다.  <sub>(11.1s)</sub>
  - 화면: fold4 막대 두 개 강조
- **[05:39.8]** 변수 제거 실험에서도 레짐 분리를 빼고 단일 LightGBM으로 바꾸면 OOF MAE가 4.348 나빠집니다. 다섯 가지 제거 실험 가운데 가장 큰 악화입니다.  <sub>(12.2s)</sub>
  - 화면: Ablation 가로막대 5개, 레짐분리 강조
- **[05:52.0]** 다섯 기준을 가중합한 선정 스코어카드에서도 0.9982로 1위였고, 2분류와 섞는 앙상블은 가중치 1.0이 최선이라 채택하지 않았습니다.  <sub>(12.9s)</sub>
  - 화면: 스코어카드 종합점수 막대 + 앙상블 미채택 줄

## caution  (06:05.0 ~ 07:28.4)

- **[06:05.0]** 자, 이제 이 결과를 읽을 때 주의할 점 세 가지를 짚겠습니다.  <sub>(5.0s)</sub>
  - 화면: 구간 제목 '읽을 때 주의할 점'
- **[06:10.0]** 첫째, 이 최종모델은 하이퍼파라미터 튜닝을 받지 않았습니다. Optuna로 찾은 최적값은 단일 LightGBM에만 들어갔고, 게이트와 레짐 회귀는 LightGBM 기본 설정 그대로입니다.  <sub>(13.8s)</sub>
  - 화면: 주의 카드 1: 튜닝 없음 (num_leaves 31, 학습률 0.1 기본값)
- **[06:23.7]** 튜닝 없이도 1위라는 건 구조의 힘을 보여 주지만, 동시에 아직 짜낼 여지가 남아 있다는 뜻이기도 합니다.  <sub>(8.5s)</sub>
  - 화면: 카드 아래 한 줄
- **[06:32.2]** 둘째, 모든 구간에서 이기는 것은 아닙니다. 폴드 3에서는 레짐 모델이 23.437로, 단일 LightGBM의 15.367보다 오히려 나쁩니다. 문서에는 폴드 4 이야기만 있으니, 이 구간도 같이 봐야 합니다.  <sub>(18.5s)</sub>
  - 화면: 주의 카드 2 + fold3 막대 비교
- **[06:50.7]** 셋째, 테스트 MAE의 13.56퍼센트 개선은 통계적으로 유의하지 않습니다. 날짜 단위 블록 부트스트랩의 95퍼센트 신뢰구간이 마이너스 0.026에서 1.925로 0을 아주 조금 포함하고, p값은 0.067입니다.  <sub>(20.6s)</sub>
  - 화면: 주의 카드 3 + 신뢰구간 수직선(0 표시)
- **[07:11.4]** 덧붙여, 복제일을 걷어낸 D2 조건에서는 2분류가 1위, 3분류가 2위로 자리가 바뀝니다. 레짐을 나눈다는 발상은 튼튼하지만, 2분류와 3분류 중 어느 쪽이 나은지는 데이터 조건에 따라 달라집니다.  <sub>(17.0s)</sub>
  - 화면: D1/D2 순위 두 줄

## outro  (07:28.4 ~ 08:01.5)

- **[07:28.4]** 정리하겠습니다. 레짐 모델은 게이트로 운전 상태를 먼저 고르고, 그 상태 전용 회귀로 전력을 예측합니다.  <sub>(9.0s)</sub>
  - 화면: 정리 1
- **[07:37.3]** 확률을 섞지 않는 하드 게이팅이라 레짐끼리 답이 섞이지 않고, 달력이 깨지는 구간에서 특히 강했습니다.  <sub>(8.4s)</sub>
  - 화면: 정리 2
- **[07:45.8]** 다만 튜닝이 없고, 폴드 3에서는 지며, 개선이 아직 유의하지 않다는 단서를 달고 읽어야 합니다.  <sub>(8.0s)</sub>
  - 화면: 정리 3
- **[07:53.8]** MAE를 어떻게 읽는지, 그리고 유의성을 어떻게 판정했는지는 평가지표 편에서 이어서 다룹니다.  <sub>(7.7s)</sub>
  - 화면: 관련 편 카드
