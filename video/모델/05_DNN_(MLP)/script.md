# DNN (MLP)

녹음용 대본 — 총 468.83초 (50컷)

> 괄호 안 숫자는 그 문장을 읽는 데 배정된 시간입니다. 크게 벗어나면 영상과 어긋납니다.

## intro  (00:00.0 ~ 00:43.3)

- **[00:00.0]** 여러분, 딥러닝을 쓰면 예측이 더 좋아질까요?  <sub>(4.1s)</sub>
  - 화면: 화면 가운데 질문 한 줄
- **[00:04.1]** 층을 여러 겹 쌓은 신경망은 이 공장 데이터에서 여섯 모델 가운데 몇 등을 할까요?  <sub>(6.4s)</sub>
  - 화면: 질문 아래 여섯 모델 이름표가 고정색으로, DNN 옆에 물음표 칩
- **[00:10.4]** 오늘은 참가계획서에 비교 모델로 적었던 DNN, 다층 퍼셉트론을 뜯어보겠습니다.  <sub>(6.6s)</sub>
  - 화면: 강의 제목 카드
- **[00:17.1]** 과제부터 짧게 짚겠습니다. 전날 자정까지의 데이터만 보고 다음 날 24시간의 시간별 평균전력을 예측하는 것이 첫 번째 과제이고,  <sub>(10.6s)</sub>
  - 화면: 과제 A 칩: 다음 날 24시간 y_avg 예측
- **[00:27.6]** 그 예측으로 15분 피크가 187킬로와트를 넘을 위험한 시간을 골라내는 것이 두 번째 과제입니다.  <sub>(8.3s)</sub>
  - 화면: 과제 B 칩: peak15 ≥ θ 판정
- **[00:35.9]** 입력은 여섯 묶음 44개 피처, 테스트용 모델의 학습 데이터는 5,599행입니다.  <sub>(7.4s)</sub>
  - 화면: fact_row 두 줄: 피처 44개(6묶음) · 테스트 학습 5,599행

## arch  (00:43.3 ~ 01:37.5)

- **[00:43.3]** 구조부터 보겠습니다. 한 시간을 예측할 때 들어가는 입력은 44개의 숫자입니다.  <sub>(7.2s)</sub>
  - 화면: 구간 제목 + 왼쪽 입력 상자 '입력 44'
- **[00:50.5]** 첫 층은 뉴런 128개짜리 완전연결층, Dense입니다. 활성화 함수로는 ReLU를 씁니다.  <sub>(7.7s)</sub>
  - 화면: Dense 128 · ReLU 상자 + 화살표
- **[00:58.2]** 그 뒤에 배치 정규화와 드롭아웃이 붙습니다. 드롭아웃 비율은 0.2입니다.  <sub>(7.2s)</sub>
  - 화면: BatchNorm, Dropout 0.2 상자(얇게)
- **[01:05.4]** 두 번째 층은 뉴런 64개, 마지막 층은 출력 하나. 44에서 1까지 좁아지는 모양이죠.  <sub>(7.8s)</sub>
  - 화면: Dense 64 · ReLU, Dense 1 상자 + 출력
- **[01:13.2]** 이 네트워크를 시간별 평균전력용과 15분 피크용으로, 똑같은 구조 두 벌을 따로 학습합니다.  <sub>(7.8s)</sub>
  - 화면: 상자 줄 뒤에 그림자 한 겹 + '× 2벌 : y_avg · y_peak'
- **[01:21.0]** 파라미터를 세어 보면 한 벌에 14,593개, 두 벌이면 29,186개입니다.  <sub>(7.5s)</sub>
  - 화면: 상자 아래 층별 파라미터 수 + 합계 줄
- **[01:28.5]** 그런데 테스트용 모델이 실제로 배우는 행은 4,760행. 파라미터가 학습 행보다 세 배 넘게 많습니다.  <sub>(9.0s)</sub>
  - 화면: fact_row: 파라미터 14,593 vs 학습 행 4,760

## toy  (01:37.5 ~ 02:38.3)

- **[01:37.5]** 뉴런 하나가 하는 일을 예시 숫자로 손계산해 보겠습니다.  <sub>(4.7s)</sub>
  - 화면: 구간 제목 + '예시 숫자' 칩
- **[01:42.2]** 입력이 1과 2, 두 칸이고 뉴런이 세 개인 작은 층이라고 하죠. 뉴런마다 입력에 가중치를 곱해 더하고, 편향을 더합니다.  <sub>(10.2s)</sub>
  - 화면: 왼쪽 입력 x=[1,2], 가운데 가중치 3행 · 편향 3개
- **[01:52.5]** 첫 뉴런은 1 빼기 2로 마이너스 1, 둘째는 0.5 더하기 1로 1.5, 셋째는 마이너스 1에 편향 0.5를 더해 마이너스 0.5입니다.  <sub>(13.2s)</sub>
  - 화면: 각 행 오른쪽에 계산식과 z 값
- **[02:05.7]** 여기에 ReLU를 씌웁니다. ReLU는 음수를 0으로 자르니, 다음 층으로 넘어가는 값은 0, 1.5, 0입니다.  <sub>(9.9s)</sub>
  - 화면: ReLU 열: 음수 두 칸은 0(회색), 1.5 만 민트
- **[02:15.7]** 우리 모델의 첫 층은 이 계산을 입력 44개, 뉴런 128개로 합니다. 뉴런마다 가중치 44개와 편향 하나라서, 5,760개입니다.  <sub>(11.9s)</sub>
  - 화면: 식: 128 × (44 + 1) = 5,760
- **[02:27.6]** 배치 정규화는 이 출력의 평균과 분산을 미니배치마다 다시 맞추고, 드롭아웃은 학습할 때만 뉴런의 20퍼센트를 무작위로 꺼 둡니다.  <sub>(10.7s)</sub>
  - 화면: 두 줄 설명(BatchNorm / Dropout) + 뉴런 점 10개 중 2개 회색

## code  (02:38.3 ~ 03:48.3)

- **[02:38.3]** 이제 코드를 보겠습니다. s05 모델 파일의 dnn_fit 함수입니다.  <sub>(5.8s)</sub>
  - 화면: 코드 창(ep05_dnn_build.py) + 파일 위치 태그
- **[02:44.0]** 먼저 시드를 고정하고, 입력 44개를 표준화합니다. 표준화의 평균과 표준편차는 학습구간에서만 구합니다.  <sub>(9.5s)</sub>
  - 화면: 8~10줄 띠 + 설명 ①
- **[02:53.5]** 반복문이 평균전력과 15분 피크, 두 타깃을 한 번씩 돌면서 타깃도 표준화하고 모델을 새로 만듭니다.  <sub>(8.4s)</sub>
  - 화면: 13~14줄 띠 + 설명 ②
- **[03:01.9]** 층 구성은 방금 본 그대로입니다. Dense 128, 배치 정규화, 드롭아웃, Dense 64, 그리고 출력 하나.  <sub>(9.3s)</sub>
  - 화면: 15~24줄 띠 + 설명 ③
- **[03:11.2]** 옵티마이저는 학습률 0.001의 Adam, 손실은 평균제곱오차입니다.  <sub>(6.7s)</sub>
  - 화면: 25줄 띠 + 설명 ④
- **[03:17.8]** 학습 부분입니다. 학습구간의 마지막 15퍼센트, 최소 24행을 떼어 조기종료 검증에 씁니다. 테스트용 모델이면 839행입니다.  <sub>(11.9s)</sub>
  - 화면: 코드 창 교체(ep05_dnn_train.py), 1줄 띠 + 오른쪽 학습/검증 막대(4,760 | 839)
- **[03:29.7]** 에폭은 최대 300, 배치는 32입니다. 검증 MAE가 20에폭 동안 나아지지 않으면 멈추고, 가장 좋았던 가중치로 되돌립니다.  <sub>(11.1s)</sub>
  - 화면: 2~12줄 띠 + 설명 ⑥
- **[03:40.8]** 마지막으로 표준화된 단위로 나온 예측을 inverse_transform으로 킬로와트 단위로 되돌립니다.  <sub>(7.5s)</sub>
  - 화면: 13~15줄 띠 + 설명 ⑦

## results  (03:48.3 ~ 05:09.1)

- **[03:48.3]** 성적을 보겠습니다. 수치는 모두 KAMP 포드의 FULL 실행본입니다.  <sub>(5.8s)</sub>
  - 화면: 출처 캡션 + 표 머리(모델 · OOF MAE · 테스트 MAE)
- **[03:54.1]** 교차검증 OOF MAE는 33.177, 테스트 MAE는 10.331. 여섯 회귀 모델 가운데 둘 다 꼴찌입니다.  <sub>(9.9s)</sub>
  - 화면: 표 6행, DNN 행에 빨간 테두리
- **[04:04.0]** 학습 없이 일주일 전 값을 그대로 쓰는 Seasonal Naive의 6.542보다도 나쁩니다.  <sub>(7.1s)</sub>
  - 화면: Naive 테스트 MAE 셀 확대
- **[04:11.1]** 폴드별로 보면, 하계휴가가 걸친 폴드 3과 4에서 43.979, 55.215까지 커집니다.  <sub>(9.3s)</sub>
  - 화면: 그림 F24(모델 × fold MAE) + 오른쪽 바깥 라벨
- **[04:20.5]** 네 폴드 모두 트리 계열 네 모델보다 나쁘고, Seasonal Naive를 이긴 것은 폴드 4 한 곳뿐입니다.  <sub>(7.4s)</sub>
  - 화면: 그림 옆 두 줄(✕ 트리 네 모델보다 4/4 폴드 나쁨 · ✔ Naive 보다 나은 곳은 폴드 4)
- **[04:27.9]** RF보다 테스트 MAE가 71.07퍼센트 나쁘고, 날짜 블록 부트스트랩의 95퍼센트 신뢰구간이 마이너스 7.411에서 마이너스 1.851로 0을 포함하지 않습니다.  <sub>(15.4s)</sub>
  - 화면: CI 수직선: 구간 전체가 0 왼쪽
- **[04:43.3]** RF와 비교한 다섯 모델 가운데 차이가 유의하게 나온 모델은 DNN 하나, 그것도 나쁜 쪽으로입니다.  <sub>(7.7s)</sub>
  - 화면: CI 아래 한 줄: 유의 = DNN 하나(나쁜 쪽)
- **[04:51.0]** 피크 판정에서는 28건을 모두 잡았지만 오경보가 72건입니다. Seasonal Naive와 혼동행렬 네 칸이 똑같습니다.  <sub>(9.5s)</sub>
  - 화면: 2×2 혼동행렬 두 개 나란히(DNN · Naive)
- **[05:00.5]** 선정 스코어카드는 0.1815로 꼴찌이고, 학습 시간은 58.36초로 가장 깁니다.  <sub>(8.6s)</sub>
  - 화면: fact_row 두 줄

## caution  (05:09.1 ~ 06:01.2)

- **[05:09.1]** 이 결과를 읽을 때 주의할 점 세 가지를 짚겠습니다.  <sub>(4.2s)</sub>
  - 화면: 구간 제목
- **[05:13.3]** 첫째, 신경망의 숫자는 실행 환경에 따라 달라집니다. 같은 파이프라인을 로컬 Windows에서 돌린 DNN은 OOF 31.038, 테스트 10.487. 포드는 33.177, 10.331입니다.  <sub>(18.4s)</sub>
  - 화면: 주의 카드 1 + 두 열 표(포드 · 로컬 Windows, '다른 환경 실행값' 칩)
- **[05:31.7]** DNN을 뺀 다섯 모델은 두 실행이 소수 셋째 자리까지 같습니다. 시드 42와 연산 결정론 설정을 켰는데도 DNN만 달라졌습니다.  <sub>(10.1s)</sub>
  - 화면: ✔ 다섯 모델 같음 / ✕ DNN 다름 + 코드 위치 태그
- **[05:41.8]** 일반적으로 이런 설정은 같은 환경 안에서의 재현을 위한 것이고, 환경이 바뀌면 보장되지 않습니다. 이 자료로 원인을 하나로 짚을 수는 없습니다.  <sub>(11.9s)</sub>
  - 화면: 회색 한 줄 '일반론' 표시
- **[05:53.7]** 그리고 프로젝트 결과 문서에 적힌 DNN 행은 이 로컬 값입니다. 이 강의는 포드 값을 씁니다.  <sub>(7.5s)</sub>
  - 화면: 문서 행 한 줄(31.038 · 10.487 · 22.226) = 로컬

## caution2  (06:01.2 ~ 07:12.2)

- **[06:01.2]** 둘째, 이 차이는 다른 모델의 점수까지 바꿉니다. 스코어카드는 기준마다 가장 좋은 값을 1, 가장 나쁜 값을 0으로 펴는데, DNN이 세 기준에서 꼴찌라 0점 끝을 쥐고 있습니다.  <sub>(14.4s)</sub>
  - 화면: 주의 카드 2
- **[06:15.6]** 피크 MAE 자를 보죠. DNN 값이 47.88에서 55.08로 바뀌면 자가 늘어나, 자기 값은 그대로인 LightGBM의 점수가 0.431에서 0.562로 올라갑니다.  <sub>(15.3s)</sub>
  - 화면: 두 개의 자(포드 · 로컬): 1점 끝 23.87, 0점 끝 47.88 → 55.08, LightGBM 눈금
- **[06:30.9]** 종합점수는 레짐 3분류만 0.9982 그대로이고, DNN을 뺀 나머지 네 모델도 모두 바뀝니다. 순위는 같아도 점수는 꼴찌 모델을 따라 움직입니다.  <sub>(12.8s)</sub>
  - 화면: 표: 모델 · 포드 점수 · 로컬 DNN 대입 점수 · 변화
- **[06:43.7]** 셋째, 5,599행짜리 표 데이터입니다. 일반적으로 이런 규모의 표 데이터에서는 트리 기반 모델이 신경망보다 강한 경우가 많다고 알려져 있고, 이 프로젝트에서도 DNN은 네 폴드 모두 트리 모델보다 나빴습니다.  <sub>(17.3s)</sub>
  - 화면: 주의 카드 3
- **[07:01.0]** 다만 DNN이 왜 뒤처졌는지는 따로 실험하지 않았습니다. 파라미터가 학습 행보다 많다는 것도 사실일 뿐, 원인으로 확인된 것은 아닙니다.  <sub>(11.2s)</sub>
  - 화면: 카드 아래 한 줄

## outro  (07:12.2 ~ 07:48.8)

- **[07:12.2]** 정리하겠습니다. DNN은 44개 입력을 128, 64 두 층으로 받아 값 하나를 내는 다층 퍼셉트론이고, 타깃마다 한 벌씩 학습합니다.  <sub>(11.4s)</sub>
  - 화면: 정리 1
- **[07:23.6]** 포드 실행에서는 OOF와 테스트 모두 여섯 모델 가운데 꼴찌였고, RF보다 유의하게 나쁜 유일한 모델이었습니다.  <sub>(8.8s)</sub>
  - 화면: 정리 2
- **[07:32.4]** 그리고 신경망의 숫자는 환경에 따라 달라지고, 그 차이가 스코어카드를 거쳐 다른 모델 점수까지 바꿉니다.  <sub>(8.6s)</sub>
  - 화면: 정리 3
- **[07:41.0]** 순환 신경망 RNN, 트리 부스팅 LightGBM, 그리고 블록 부트스트랩은 관련 편에서 이어서 다룹니다.  <sub>(7.8s)</sub>
  - 화면: 관련 편 카드
