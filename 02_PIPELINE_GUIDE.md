# PIPELINE GUIDE — 코드가 무엇을 왜 하는지

노트북 164셀이 0~10장으로 나뉩니다. 각 장이 **무엇을 하고, 왜 그렇게 해야 하며,
보고서 어느 절에 대응하는지** 정리했습니다.

이 문서의 목적은 "코드를 읽으면 알 수 있는 것"을 반복하는 게 아니라,
**코드만 봐서는 알 수 없는 설계 근거**를 남기는 것입니다.

---

## 전체 그림

| 장 | 하는 일 | 보고서 | 입력 |
|---|---|---|---|
| 0 | 실행 환경·재현성 설정 | 6장 | — |
| 1 | 데이터 이해 및 진단 | 1.2~1.4 | 원본 CSV |
| 2 | 파생변수 구성 + **시간누수 차단** | 1.5, 2.3 | 1장 정비 결과 |
| 3 | 분할 설계 및 평가지표 | 1.6, 2.1 | 2장 피처 |
| 4 | 가이드북 베이스라인 재현 | **2.2** | 3장 분할 |
| 5 | 제안모델 개발 (8종) | 2.4, 2.5 | 3장 분할 |
| 6 | 성능평가 및 최종모델 선정 | **2.6~2.9** | 5장 결과 |
| 7 | 영향요인 및 오류분석 | **3장** | **OOF 예측** |
| 8 | 피크 저감 시뮬레이션 | **4장** | **전체기간 실측** |
| 9 | 창의성·차별성 근거 | 5장 | 2~8장 수치 |
| 10 | 산출물·재현성·패키징 + **모델 저장(서비스 번들)** | 6장 | 전체 |

7장과 8장의 **입력이 다른 것**이 핵심입니다. 이유는 아래에 있습니다.

---

## 1장 — 데이터 이해 및 진단

이 데이터에는 표준 점검으로는 **절대 드러나지 않는** 문제가 둘 있습니다.

### (1) 데이터의 44.7% 가 합성 복제일입니다

파일명 `okm_**augumented**_2021.csv` 가 시사하듯, **24시간 전력 프로파일이 바이트 단위로
동일한 날**이 대량 존재합니다.

- 고유 프로파일 **142 / 257일**, 중복그룹 45개, 잉여(복제) **115일 = 44.7%**
- 최대 그룹은 15일인데, 그날들의 기온은 **−3.4°C ~ 19.2°C** 로 완전히 다릅니다
- **행 단위 완전중복은 0건**이라 `df.duplicated()` 로는 아무것도 못 찾습니다

그래서 **일별 전력 프로파일을 해시**해서 탐지합니다 (`detect_profile_duplicates`).
이것을 모르면 fold 검증이 "일반화"가 아니라 "암기"를 측정하게 됩니다.

### (2) 결측이 양방향입니다

| | 센서측 | ERP측 |
|---|---|---|
| 시점 | 08-28 18시 ~ 08-29 10시 (17시간) | 7/13 · 7/15 (48시간) |
| 증상 | 평균전력 0 + 공장인원 NaN | 생산량 0 + 공장인원 0 + 인건비 전부 1.5 |
| 전력 | 계측 정지 | **136 (정상 가동 수준)** |
| 처리 | 보간 + `is_outage` → **평가에서 제외** | `is_erp_missing` → **평가·학습 모두 제외** |

ERP 결측 48행은 `생산량=0` 이라 2단계 레짐의 게이트가 '비가동'으로 분류하는데
실제 평균전력이 135.9(정상 비가동은 44.8)여서 **비가동 회귀를 오염**시킵니다.
그래서 학습에서도 뺍니다.

> **야간 피크 해석 주의**: 야간(19시 이후) 피크 7건이 **전부 이 두 날짜**에서 나옵니다.
> 정상 가동일 기준으로는 야간 피크가 0건입니다. 이 문장이 3.6절 규칙과 4장 L4 레버의
> 근거이므로 보고서 서술을 이에 맞춰야 합니다.

### (3) 추가 발견 — 증강의 '채널 불일치'

생산량이 0인데 전력이 정상 가동 수준(≥70kW)인 날은 **11일**입니다. 7/13·7/15 만이 아닙니다.
`인건비` 컬럼이 두 집단을 갈라 줍니다.

| | ERP측 결측 (2일) | 증강 채널 불일치 (9일) |
|---|---|---|
| 날짜 | 7/13 · 7/15 | 1/10, 1/24, 2/11, 3/1, 3/7, 3/14, 3/21, 3/28, 5/9 |
| `인건비` | **전부 1.5 (손상)** | 정상 (1.0/1.5 혼재) |
| 복제그룹 | 미소속 | **대부분 소속** |
| 요일 | 화·목 | **대부분 일요일 + 법정공휴일** |
| 해석 | 실제 가동 중 ERP 기록 누락 | **평일 전력 프로파일이 증강으로 덮어씌워짐** |

일요일 37일 중 8일이 일평균전력 ≥70kW 인데 일요일 중앙값은 22.9kW 입니다.

➡️ **2/11(설날)·3/1(삼일절)은 공휴일이 맞습니다.** 전력만 보면 "정상 가동일"로 보이지만
생산량·공장인원이 0이고 복제그룹에 속합니다. **전력 단일 채널로 가동/휴무를 판정하면
증강 산물을 실재 패턴으로 오인합니다.** 그래서 가동 캘린더는 `일생산량 == 0` 으로 판정합니다.

### (4) 피크 정의 — 이것만은 꼭

```
peak15 = max(15분, 30분, 45분, 60분)
```

**이 정의에서만** 보고서의 q95=187 / q99=201 / max=222 가 재현됩니다.
`15분` 단일 컬럼은 **173.4 / 188 / 207** 로 불일치합니다.
가이드북 베이스라인은 `15분` 만 쓰므로 **따라가면 안 됩니다.**

---

## 2장 — 시간누수 차단

### 예측 원점을 `D일 24:00` 으로 두는 이유

```
origin(D+1) = D일 24:00   (= D+1일 00:00)
```

초안의 `D일 23:00` 은 **결함**입니다. `h=23` 을 예측할 때 `lag24` 의 참조 시각이
`D일 23:00` 인데, 이 값은 23:00~24:00 **구간의 집계**여서 그 시점에는 **아직 미완성**입니다.
원점을 24:00 으로 두면 참조 시각과 원점의 간격이 `h` 가 되어 모든 `h` 에서
`k ≥ 24` 인 lag 가 엄밀히 안전해집니다.

### 이동통계 금지 패턴

`rolling(24).mean().shift(1)` 은 `h>0` 에서 **같은 날의 과거 시각**을 창에 포함시켜
누수가 됩니다. 반드시 **원점에서 1회 계산해 그날 24시간에 동일 값을 뿌립니다.**

검증: "원점 기준 이동통계는 하루 안에서 값이 변하지 않는다" 를 테스트로 확인합니다.
하루 안에서 변하면 그게 곧 누수입니다.

### 누수 검증을 2층으로 하는 이유

| 층 | 방법 |
|---|---|
| (A) 관측완료 시각 검사 | 각 피처의 **관측완료 시각**(참조시각+1h)이 원점 이하인지 |
| (B) **미래맹검 동등성** | 대상일의 전력 컬럼을 **전부 NaN 으로 지우고** 피처를 다시 만들어 원본과 완전 일치하는지 |

(A) 만으로는 `rolling().shift(1)` 이나 브로드캐스트 실수를 놓칩니다. (B) 는 모델과 무관하게
자동 적발합니다. 값이 하나라도 달라지면 그 피처는 대상일의 전력을 보고 있었다는 뜻입니다.

**탐지기 자체를 검증합니다**: 테스트에 음성 대조가 있어, 일부러 누수를 주입하면
탐지기가 반드시 실패하는지 확인합니다. 통과만 하는 검사는 아무것도 보장하지 않습니다.

---

## 3장 — 분할과 지표

### θ(정의) 와 τ(판정) 를 기호부터 분리하는 이유

| 기호 | 의미 | 산출 범위 | 값 |
|---|---|---|---|
| **θ** | 피크 **정의** 임계값 | 학습 전체구간 q95에서 **1회** | **187 고정** |
| **τ** | **판정/경보** 임계값 | **각 fold 검증구간에서만** | 모델·fold별 가변 |

fold별 학습구간 q95 는 **182 / 182 / 184 / 186 / 186** 으로
**어느 fold 에서도 187이 나오지 않습니다.** θ를 fold 마다 재산출하면 `y_cls` **정의 자체가
달라져** fold 간 Recall 평균이 무의미해집니다. **θ 고정은 선택이 아니라 필수입니다.**

τ 는 회귀→판정 경로에 필수입니다. 회귀 예측은 평균으로 수축하므로 θ를 그대로 쓰면
구조적으로 불리합니다. 그래서 2.7절 표에 **각 행이 쓴 τ를 컬럼으로 노출**합니다.

### fold1 을 버리는 이유

복제 오염을 실측하면 fold1 검증 14일 중 **7일(50%)** 의 전력 프로파일이 이미
학습구간에 있습니다. 이 fold 의 성능은 일반화가 아니라 암기입니다. fold2~5 는 오염 0% 입니다.

### Task B 주지표를 Recall 에서 F1/PR-AUC 로 바꾸는 이유

달력규칙 한 줄(`평일 ∧ 08≤h≤18`)이 테스트에서 **Recall 1.000** 을 냅니다
(TP 28 / FN 0 / FP 82 / TN 226). **어떤 ML 모델도 Recall 로는 이길 수 없습니다.**

그래서 주지표를 **"Recall 유지 + 오경보(FP) 최소화"** 로 재정의하고, 제안모델의 기여를
**"동일 수준에서 FP 82건 → N건"** 으로 정량화합니다. 이 기준 행이 없으면
"Recall 0.9 달성"이 실제보다 대단해 보이는 착시가 생깁니다.

---

## 4장 — 가이드북 베이스라인 재현

결함 **13건**을 재현·진단합니다. 그중 **무음 미반영(silent no-op) 3건**이 특히 위험합니다 —
예외를 던지지 않으므로 "무오류 완주"만 확인하면 절대 발견되지 않습니다.

| 셀 | 결함 | 영향 |
|---|---|---|
| **73** | `temp = ...iloc[0]` (루프 변수가 아님) | **치명적** — 전 행의 외생변수가 1행 값으로 고정 → **실질 피처 1개** |
| 32 | `df['Weekend'][mask] = 1` | 무음 미반영 — Weekend 전부 0 |
| 34 | `df['Vacation'].loc[gun] = 1` | 무음 미반영 — Vacation 전부 0 |
| 48 | `fit_transform(전체 데이터)` | 테스트 분포가 스케일러에 유입 |
| 52 | `reshape((N,24,7))` | C-order 로 접어 **시간축이 역순** |

**버그 서명**: 학습 MSE **186.12** > 테스트 MSE **183.65**. 과적합이 전혀 없다는 것은
모델이 학습할 것이 1개뿐이었다는 뜻입니다. 피처 중요도가 단일 변수에 **1.0000** 집중되는
것으로 증명합니다(그림 F16).

---

## 5장 — 제안모델 8종

Seasonal Naive · Random Forest(보정) · LightGBM · 2단계 레짐(2분류/3분류) ·
피크 직접분류 · DNN(MLP) · 달력규칙.

### 가장 중요한 결과 — fold4 에서 갈립니다

| 모델 | fold2 | fold3 | **fold4** | fold5 |
|---|---|---|---|---|
| Seasonal Naive | 7.5 | 24.4 | **69.3** | 8.3 |
| 2단계 레짐(2분류) | 7.3 | 23.9 | **10.4** | 5.9 |

fold3·fold4 는 **하계휴가(07-31~08-08)와 재가동**을 포함합니다.
`lag168` 의 참조 시각이 휴무일이 되면 Naive 는 완전히 붕괴합니다
(`08-09 10시` 실측 182 vs lag168 참조 22).

➡️ **제안모델의 이점은 평균이 아니라 '달력이 깨지는 구간'에 집중됩니다.**
이것이 2.9절 선정 근거와 7장 오류분석의 핵심입니다.

---

## 6장 — 평가와 선정

**평가 층위를 분리합니다.**

| 층위 | 용도 | 양성 표본 |
|---|---|---|
| **Rolling-origin OOF** | **모델 비교의 주 근거** | 156건 |
| 테스트 336시간 | 최종 확인용 단일 수치 | 28건 |

테스트 피크 28건은 8개 날짜·16개 연속블록에만 분포해 **유효 표본이 8~16** 에 가깝습니다.
Recall 기준 Clopper-Pearson 95% CI 폭이 **0.37** 이라 **테스트만으로는 두 모델을 구분할 수
없습니다.** 그래서 선정은 OOF 기준으로 합니다.

### Ablation 에서 나온 뜻밖의 결과 — 6.6절에서 분해해 검증

**과거 전력 지연변수를 제거하면 OOF MAE 가 오히려 개선됩니다(−3.131).** 이것이 최종모델을
바꿀 근거인지 6.6절이 **테스트를 보지 않고, 결과를 보기 전에 정한 기준 ①~⑤** 로 판정합니다
(변형 62개 + D2 재확인. 잡음 폭 = 난수 변수 1개를 추가한 실행 5회의 |ΔMAE| 최댓값).

- 개선은 **최종모델 오차 상위 2일(8/2·8/3, 하계휴가 중 평일)** 에서 나옵니다. 1단계 분류기가
  계획상 휴무일을 가동 레짐으로 보내(6.6절 게이트 배정표) OOF 절대오차의 34% 가 이 이틀에 몰렸고,
  지연변수를 빼면 이 이틀이 맞으면서 전체 MAE 가 내려간 것입니다.
- 두 날을 제외하면 지연변수를 뺀 변형이 **오히려 나쁘고(+0.61)**, 가동하지만 저부하인
  **토요일의 MAE 가 7.45 → 18.96** 으로 커집니다. 지연변수는 정상 운영에 필요합니다.
- 기준 ①~④ 를 통과한 피처군 제거 변형은 **0개**. 휴무 인지형 지연변수도 시험해 기각했습니다.
  44개 변수 개별 제거는 대부분 잡음 폭 수준입니다.
  - 풍속·peak15 24시간 전 제거 2개가 형식상 ①~④ 를 통과했지만, 효과가 잡음 폭 바로 위(−0.22)입니다.
  - 44개를 동시에 비교한 결과라 교체 근거로 쓰지 않습니다(D2 결과는 03_RESULTS §4).
- 결론: **44개 피처를 유지**합니다. 개선 대상은 변수가 아니라 1단계 분류기의 휴무 판정입니다.
- 이 절의 원래 실험과, 휴무 판정 수정안을 비교한 후속 실험은 [`experiments/`](experiments/README.md) 에 격리해 두었습니다(저장소에만 있음 · 제출 zip 미포함).
  - 결론: 수정안은 모두 ERP 결측 가동일에서 실패해 제출 모델은 그대로 둡니다.
  - 서비스에는 확정 휴무일에만 거는 보정 옵션을 제안합니다.

반면 **레짐분리 제거는 MAE 를 가장 크게 악화**시킵니다 — 전체 피처군 중 기여 1위입니다.

---

## 7장 — 입력이 **OOF** 인 이유

테스트 336시간에는 피크가 28건뿐이고 **7개 조건 × FN 6~8건 → 셀당 0~2건**이라
3.4절 "전체 미탐지의 N%" 가 단일 사례로 결정됩니다. 게다가 테스트는 9월뿐이라
**'여름 vs 그 외' 조건이 성립하지 않습니다.**

➡️ 규칙을 고정합니다: **6장 성능표(헤드라인) = 테스트 336시간 / 7장 분석 전부 = OOF.**
모든 조건별 표에 **표본수 n 과 신뢰구간을 병기**합니다.

### SHAP 적용 경로

2단계 레짐 모델은 TreeExplainer 를 직접 적용할 수 없습니다(설명 대상 단일 트리가 없고,
1단계 log-odds 와 2단계 kW 의 단위가 불일치). 그래서:

- **전역 변수 순위** = 파이프라인 전체에 대한 **permutation importance** (모델 구조 무관)
- **SHAP** = 방향성 보조. 대리모델(단일 LightGBM)을 쓰고 **대리 충실도를 함께 보고**

---

## 8장 — 입력이 **전체기간 실측** 인 이유

### 9월만으로는 절감액이 논리적으로 0원입니다

요금이 **"3개월 최대피크로 12개월 청구"** 구조입니다.

| 월 | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 | **9(테스트)** |
|---|---|---|---|---|---|---|---|---|---|
| 최대 peak15 | **222** | 198 | **222** | 199 | 199 | **222** | **222** | 218 | **204** |

7~9월 창의 최대는 **222(7월)** 입니다. 9월 peak15 를 **전부 0으로 낮추는 극단 가정에서도**
연간 기본요금 절감액은 **0원**입니다. 그래서 시뮬레이션은 **전체기간(01-01~09-14) 실측
`peak15` 시계열** 위에서 하고, 9월 예측 결과는 운영 시나리오 검증용으로만 씁니다.

### 최대값 목적함수의 성질 — 이 장의 핵심 발견

청구 피크는 **최대값**이므로 **상위 사건을 하나라도 남기면 절감이 0** 입니다.

| 레버 | 대상 시각 | Δ최대수요 |
|---|---|---|
| L1a (계획서 명세) | 08·13시 | **0.0 kW** |
| L1b | + 11시 | **8.0 kW** |
| L1c | + 09시 | **13.0 kW** |

L1a 가 0인 이유: 최대값 222 kW 4건 중 3건은 08시이지만 **1건(07-19 11시)이 11시**입니다.
이 한 사건이 남는 순간 청구 피크가 그대로 유지됩니다. 11시를 덮으면 이번에는
09시 사건(07-21 09시 = 214 kW)이 새 상한이 됩니다.

➡️ **상위 피크 사건을 순차적으로 모두 덮어야 추가 저감이 생깁니다.**

### 물량 이동보다 동시성 분산이 핵심입니다

pooled 회귀(시간 고정효과 포함)의 생산량 계수는 **β ≈ 0.001 kW/생산1단위** 입니다.
20 kW 를 낮추려면 약 2만 단위를 옮겨야 하는데 하루 총 생산량을 넘습니다.

➡️ 이 공장의 peak15 는 생산 **'물량'** 보다 **설비 '동시 가동'** 이 지배합니다.
따라서 저감의 핵심 레버는 물량 재배치(L2·L3)가 아니라 **동시성 분산(L1)** 입니다.
이는 **생산 일정을 크게 흔들지 않고도 피크를 관리할 수 있다**는 뜻이라 현장에 유리한 결론입니다.

모든 레버는 **일일 총 생산량을 보존**합니다 — 생산을 줄이는 게 아니라 옮기므로 매출 손실이 없습니다.
단 L4b(주말 이전)는 날짜를 넘으므로 일 단위 보존이 깨지고, 표에 `False` 로 정직하게 표시됩니다.

L4(야간 이전)는 인건비가 1.5배가 되어 **순절감액이 음수**입니다.
"무조건 좋은 레버가 아님"을 수치로 보입니다.

---

## 0장·10장 — 재현성과 패키징

| 장치 | 내용 |
|---|---|
| 시드 고정 | `PYTHONHASHSEED=42`, `random`/`numpy`/`keras` 시드 42 |
| 결정적 연산 | `TF_ENABLE_ONEDNN_OPTS=0`, `TF_DETERMINISTIC_OPS=1`, `enable_op_determinism()` |
| LightGBM | `deterministic=True`, `force_row_wise=True`, **`num_threads=4` 고정**(심사 PC 코어 수와 무관) |
| 한글 폰트 | 폴백 체인 + 선택 폰트 출력 |
| TF 미설치 방어 | SimpleRNN·DNN 만 건너뛰고 파이프라인 완주 |
| 무음 실패 승격 | 연쇄 대입을 `ChainedAssignmentError` 로 즉시 중단 |

### 10.5절 — 모델 저장(서비스 번들)

노트북은 학습·평가만 하고 모델을 버리던 구조였습니다. 10.5절이 **서비스 번들 두 개**를
`outputs/models/{full|fast}/{eval|deploy}/` 에 저장하고, 그 자리에서 검증합니다.

| 번들 | 학습 구간 | 역할 |
|---|---|---|
| `eval` | ~ 2021-08-31 (5,599행) | 제출 예측을 만든 **바로 그 적합 객체**. 재로드 예측 = 제출 파일(비트 단위) |
| `deploy` | ~ 2021-09-14 (5,935행) | 배포용 재학습. **보고서 수치에 쓰지 않음** |

구성: 최종모델(2단계 레짐 3분류: 게이트 + 레짐별 회귀 6 + fallback 2) · 피크 직접분류기 ·
Isotonic 보정기 · 분위 q10/q50/q90 · 예측구간 반폭 · τ·θ·피처 계약 · golden 자가검증 사례.

**저장 형식을 이렇게 정한 이유** — 코드만 봐서는 알 수 없는 함정들입니다.

| 결정 | 근거 (실측) |
|---|---|
| pickle 금지 | 레짐 모델의 예측 함수가 **클로저**라 pickle 자체가 안 된다(`Can't pickle local object`). 노트북에서 정의한 객체는 `__main__` 에 묶여 서비스에서 로드할 수도 없다 |
| LightGBM 네이티브 텍스트 | 결정적 설정에서 **재적합하면 바이트까지 같다** → 재학습 경로의 결정성을 sha256 로 증명한다 |
| 바이트 → sha256 → `model_str` | 파일 경로로 여는 LightGBM API 는 **한글이 든 절대경로를 열지 못한다**(`Could not open`) |
| 예측 직전 컬럼 재정렬 + float64 | LightGBM 은 컬럼 순서가 바뀌어도 **오류 없이 틀린 값**을 낸다. 피처는 끝까지 float64 로 다룬다(float32 로 한 번 내려가면 정밀도가 손실돼 336행 중 96행, 최대 3.35 kW 가 달라진다) |
| 매니페스트도 해시 | τ·구간 반폭은 매니페스트 안에만 있다. `manifest_sha256` 을 두고 `bundle_id` 가 그 앞 12자리라, 임계값을 고치면 로더가 거부한다 |
| 하이퍼파라미터는 dict → JSON | 모델 텍스트의 파라미터는 6자리로 반올림돼 재학습 레시피로 쓸 수 없다 |
| τ 는 반올림 없이 저장 | 표의 172.2 는 표시값이고 실제는 `172.23000000000002` — 예측이 그 사이에 들면 라벨이 뒤집힌다 |
| FAST 번들 분리 | 테스트(FAST)가 FULL 번들을 덮어쓰지 못하게 `fast/` 에 둔다. 서빙 로더는 FAST 번들을 거부한다 |

**배포 번들의 교차검증 산출물은 상속합니다.** τ·τ_cls·보정기·예측구간 반폭은 fold 검증구간에서
나온 값이라 재학습 구간(9월 포함)에서는 다시 만들 수 없습니다. 평가 번들 값을 묶음으로 쓰고
매니페스트 `inherited_from` 에 출처를 남깁니다. 반면 `scale_pos_weight` 와 분위 모델은 같은
레시피로 재학습 구간에서 다시 계산합니다.

**서비스 예측구간은 5.8절 conformal 이 아닙니다.** 5.8절 구간은 최종모델이 아닌 별도 point
모델이 중심이라, 서비스에서는 **최종모델 예측을 중심**으로 OOF 잔차 90% 분위를 휴무일·운영일
따로 씁니다(휴무일 잔차가 훨씬 크다). 5.8절 표는 그대로 둡니다.

**검증 사슬** (하나라도 실패하면 노트북이 멈춘다 — 게이트 1~4 와 같은 방식)

1. 평가 번들을 디스크에서 다시 읽은 예측 = 메모리 예측(제출 예측을 만든 값), `np.array_equal`
2. 제출 파일과 반올림 기준 일치 (최종모델이 번들 모델일 때)
3. 같은 cutoff 로 다시 적합한 모델 텍스트의 sha256 = 평가 번들 → 배포 번들의 적합 경로가 검증된 경로다
4. 번들 JSON 에 절대경로·계정명 0건 (블라인드)

**서빙 코드는 한 벌입니다.** 서빙 패키지(`serving/`)는 src 를 import 할 수 없습니다(import 하면
장 전체가 실행된다). 그렇다고 피처 함수를 손으로 복사하면 반드시 드리프트합니다. 그래서
`tools/build_serving_core.py` 가 src 의 순수 함수 **원문**을 AST 로 추출해 `serving/_core.py` 를
생성하고, 테스트가 동기화를 강제합니다. `bundle_predict`(레짐 라우팅)도 10.5절의 원문을 그대로 씁니다.

**서빙은 구조상 인과적입니다.** 서빙은 원점(D일 24:00)까지의 이력만 받아 **이력에만** 정비
(계측정지 보간)를 하고, 계획 행(대상일)에는 전력이 없습니다. 반면 배치 파이프라인은 전체 데이터에
양방향 보간을 하므로 **3일이 미래 정보를 봅니다** — 08-29(계측정지 구간이 08-29 11시 값으로 채워짐,
fold5 검증 13행)과 07-13·07-15(ERP 결측 판정에 당일 전력 사용). 9월 테스트 구간은 0건이라
서빙 리플레이가 제출 예측과 비트 단위로 같습니다. 이 목록은 테스트가 고정합니다.

운영 방법은 [`serving/README.md`](serving/README.md) 를 보세요.

### 빈칸 점검을 2단계로 하는 이유

① 정규식만으로는 부족합니다. 단순 `[TBD]` grep 은 `[TBDd]`·`[TBDDd]`·`[Tbdd]`·`[TBdd]`·
대괄호 없는 `TBd`·내부 공백이 있는 `[최종모델명 ]` 을 전부 놓칩니다.

② 더 중요한 건 **4·5·6장은 `[TBD]` 토큰이 0개**라는 점입니다. 정규식만 돌리면
**30점이 통째로 비어 있어도 통과**합니다. 그래서 장별 충족 체크리스트가 필수입니다.

---

## 코드를 고치려면

`src/sNN_*.py` 가 **단일 진실원**이고 노트북은 빌드 산출물입니다.

```bash
# 1) src/ 를 고친다
# 2) 노트북 재조립 (+ serving/_core.py 재생성)
bash run.sh build
# 3) 테스트 (⚠️ outputs/ 를 FAST 로 덮어씀 → 4)에서 FULL 로 복구)
bash run.sh test
# 4) 실행
bash run.sh run          # finalize 까지 자동 수행
# 5) 커밋된 결과와 비교 (수치 변화 0 확인, 재실행 없음)
bash run.sh verify-head
```

노트북 셀을 직접 고치면 다음 빌드에서 덮어써집니다.

`src/` 파일은 jupytext **percent 포맷**입니다 (`# %%` = 코드셀, `# %% [markdown]` = 마크다운셀).
`# %% tags=["nb-strip"]` 로 표시된 셀은 장 간 변수 전달용 import 라서
노트북 조립 시 **삭제**됩니다(노트북에서는 같은 커널에 변수가 이미 있으므로).
그래서 완성된 노트북은 어떤 모듈도 import 하지 않는 **자기완결 노트북**입니다.
