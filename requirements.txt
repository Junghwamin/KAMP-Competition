# 제6회 K-인공지능 제조데이터 분석 경진대회 ⑤ 자원 최적화 — 런타임 의존성
#
# ⚠️ 이 파일은 **사람이 관리**한다. 노트북이 덮어쓰지 않는다.
#    노트북은 실행 환경의 실제 설치 버전을 `outputs/requirements_generated.txt` 에
#    증거로 남긴다 (보고서 6장 근거용).
#
# 검증 환경: Python 3.11.9 (Windows 11).  허용 범위: Python 3.10 ~ 3.12
#   - pandas 3.0 은 Python >= 3.10 필요
#   - tensorflow 2.21 은 Python <= 3.12 지원
#
# 모든 버전을 `==` 로 완전 핀했다. 재현성 검증(22개 표 1,988개 셀 소수 6자리 일치)이
# 이 조합에서 이뤄졌으므로 버전을 바꾸면 수치가 달라질 수 있다.

# ── 수치 계산 기반 (가장 먼저 설치할 것) ─────────────────────────────
numpy==2.3.5
pandas==3.0.5

# ── 모델 ──────────────────────────────────────────────────────────────
scikit-learn==1.9.1
scipy==1.17.1
lightgbm==4.7.0
optuna==5.0.0
statsmodels==0.14.6

# TensorFlow 는 SimpleRNN·DNN(MLP) 전용이며 **GPU가 필요하지 않다**.
# 미설치 환경에서도 해당 두 모델만 건너뛰고 파이프라인이 완주한다.
# GPU 이미지에서도 평범한 wheel 로 충분하다 (tensorflow[and-cuda] 불필요).
tensorflow==2.21.0

# ── 해석·시각화 ───────────────────────────────────────────────────────
matplotlib==3.11.1
shap==0.51.0

# ── 산출물 생성 ───────────────────────────────────────────────────────
python-pptx==1.0.2
nbformat==5.10.4

# ── 노트북 커널 (Jupyter 로 열거나 nbconvert 로 실행하려면 필요) ────────
ipykernel==7.1.0

# 공휴일은 외부 패키지(holidays/workalendar) 대신 2021 상수 리스트를 사용한다
# (폐쇄망 심사환경 대응). 따라서 관련 의존성이 없다.
