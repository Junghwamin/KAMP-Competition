# RESULTS — 결과와 한계

FULL 모드(Optuna 100회, RNN 500에폭) 실행 결과입니다.
수치의 출처는 모두 `outputs/tables/` 의 CSV 이며, `outputs/report_tbd_filled.md` 에
보고서 빈칸별 대응표가 있습니다.

---

## 1. 헤드라인

| 항목 | 값 |
|---|---|
| **최종모델** | **2단계 레짐 모델 (3분류)** — 가동상태를 먼저 분류한 뒤 상태별 회귀 |
| 테스트 MAE | **5.220** kW |
| 최선 베이스라인 | Random Forest(보정) **6.039** kW |
| **개선율** | **+13.56%** |
| Peak-MAE | 14.780 kW |
| 피크 탐지 Recall / F1 | **0.929 / 0.525** |
| 하이퍼파라미터 탐색 | **100회** (회귀 50 + 분류 50, Optuna TPE) |
| 336시간 배치 추론 | **7.43초** (10회 중앙값, 실행마다 벽시계 변동 3~8초 — 이번 값은 다른 실행과 CPU 를 나눠 쓴 상태 · 모델 재적합을 포함한 측정치 — 저장된 번들로 예측만 하면 16.3ms) |
| 최대수요 저감 | **13.2 kW** (전체기간 기준) |

### 모델별 비교 (테스트 336시간)

| 구분 | 모델 | OOF MAE | 테스트 MAE | Peak-MAE |
|---|---|---|---|---|
| **제안** | **2단계 레짐(3분류)** | **11.007** | **5.220** | 14.780 |
| 제안 | 2단계 레짐(2분류) | 11.670 | 5.390 | 14.780 |
| 제안 | LightGBM | 15.355 | 5.475 | 15.818 |
| 가이드북 | Random Forest(보정) | 19.478 | 6.039 | 14.276 |
| 기준 | Seasonal Naive | 28.372 | 6.542 | 9.500 |
| 계획서 | DNN (MLP) | 31.038 | 10.487 | 22.226 |

원본 표: `outputs/tables/ch2_regression.csv`

---

## 2. 정직하게 밝혀 둘 한계 4가지

성과를 부풀리지 않는 것이 이 프로젝트의 설계 원칙입니다. 아래 네 가지는
숨기면 나중에 더 큰 문제가 되는 사실입니다.

### ① MAE 개선이 통계적으로 유의하지 않습니다

일(day) 단위 블록 부트스트랩 결과:

| 모델 | 개선율 | 차이 95% CI | p-value | 유의 |
|---|---|---|---|---|
| 2단계 레짐(3분류) | +13.56% | **[−0.026, 1.925]** | **0.067** | **아니오** |
| 2단계 레짐(2분류) | +10.75% | [−0.215, 1.865] | 0.199 | 아니오 |
| LightGBM | +9.33% | [−0.245, 1.745] | 0.263 | 아니오 |

점추정으로는 개선이지만 **CI 가 0을 (아주 좁게) 포함**합니다. p=0.067 은 0.05 에 근접하나
미달입니다. **반올림해서 "유의하다"고 쓰면 안 됩니다.**

원인은 검정력입니다. 테스트 구간이 14일이라 **일 블록이 14개**뿐입니다. 시간별 오차는
하루 안에서 강하게 상관되므로 시간 단위로 재표집하면 구간이 부당하게 좁아집니다.
그래서 블록 부트스트랩을 쓰는데, 그러면 유효 표본이 14가 됩니다.

➡️ **대응**: 선정 근거를 MAE 단독에 두지 않고 **Peak-MAE · 오경보(FP) 감소 · 폴드 편차**로
분산했습니다. 모델 비교의 주 근거도 테스트(양성 28건)가 아니라
**Rolling-origin OOF(양성 156건)** 입니다.

### ② 데이터의 44.7% 가 합성 복제일입니다

- 고유 전력 프로파일 **142 / 257일**, 잉여(복제) **115일**
- 최대 복제그룹 15일의 기온은 **−3.4°C ~ 19.2°C** 로 전혀 다릅니다
- 행 단위 완전중복은 0건이라 `duplicated()` 로는 잡히지 않습니다

추가로 **증강 채널 불일치 9일**을 발견했습니다. 생산량·공장인원이 0인데 전력만 평일
수준인 날들로, 대부분 일요일·법정공휴일이며 복제그룹에 속합니다. 즉 **평일 전력
프로파일이 증강 과정에서 덮어씌워진** 것입니다.

특히 **2/11(설날)·3/1(삼일절)은 공휴일이 맞습니다.** 전력만 보면 "정상 가동일"로
보이지만 생산량·공장인원이 0입니다. 전력 단일 채널로 판정하면 증강 산물을 실재 패턴으로
오인합니다.

➡️ **대응**: 복제일 처리를 두 조건(**D1** 유지 / **D2** 제거)으로 **모두 본실험**해
순위가 보존되는지 확인했습니다. 순위상관 **0.94** 로 결론이 강건합니다.
실질 학습 표본이 명목의 약 55% 임을 한계로 명시합니다.

### ③ 9월 테스트 구간만으로는 기본요금 절감이 0원입니다

요금이 **"3개월 최대피크로 12개월 청구"** 구조입니다.

| 월 | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 | **9(테스트)** |
|---|---|---|---|---|---|---|---|---|---|
| 최대 peak15 | **222** | 198 | **222** | 199 | 199 | **222** | **222** | 218 | **204** |

7~9월 창의 최대는 222(7월)입니다. **9월 peak15 를 전부 0으로 낮추는 극단 가정에서도
연간 기본요금 절감액은 0원**입니다.

➡️ **대응**: 시뮬레이션 대상을 **전체기간(01-01~09-14) 실측 `peak15`** 로 확대했습니다.
9월 예측 결과는 "운영 시나리오 검증"으로만 제시합니다.

### ④ 1단계 분류기가 계획상 평일 휴무를 '가동'으로 보냅니다

하계휴가 중 평일인 **8/2·8/3** 은 계획 생산량이 0 인데, 최종모델의 1단계 분류기가 08~17시를
전부 가동 레짐으로 보내 08~17시 평균 약 164 kW(시간별 최대 181 kW)를 예측했습니다(실측 21). 이 이틀(OOF 1,279시간 중 48시간)이
**OOF MAE 11.007 중 3.76 kW(34%)** 를 차지합니다(6.6절 출력. 날짜별 게이트 배정은 `ch2_gate_diagnosis.csv`).
분류기는 휴무일 변수로도 분기하지만 이 두 날에는 7일 전 가동값 같은 다른 신호에 밀렸습니다.

➡️ **대응**: 제출 모델은 고치지 않았습니다(고치면 보고서 전체 수치가 바뀝니다).
수정안은 [`experiments/2026-09-25_gate_fix/`](experiments/2026-09-25_gate_fix/RESULTS.md) 에서 검증했습니다(저장소에만 있음 · 제출 zip 미포함).
판정 기준을 먼저 적어 둔 실험이었습니다.

- **사전 판정은 채택 없음**이었습니다. 계획 휴무를 믿게 하는 수정은 방법과 관계없이 **계획이 틀린 날** 실패합니다.
  - 시험한 방법: 휴무일 기저 강제, 분류기 입력 제한, 증강 불일치일 제외 학습, 가동만 막기
  - ERP 에 생산 0 으로 기록됐지만 실제로 가동한 7/13·7/15(136 kW)를 현행은 일평균 135~140 kW 로 맞히는데, 수정안들은 약 22~35 kW 로 예측합니다.
  - 부분 가동 공휴일(설 2/12, 47 kW)도 과소예측합니다.
- 유력한 원인은 학습 데이터의 증강 불일치일(2/11·3/1 등, 생산 0 인데 전력은 가동 수준)입니다. 분류기가 여기서 계획 휴무를 덜 믿게 배웠습니다.
- 따라서 운영에서 "ERP 휴무 계획 우선" 은 **완전 휴무가 명시적으로 확정된 날로 좁혀야** 합니다.
  - 서비스 제안: 확정일에만 거는 보정 옵션(기본 꺼짐) — [`PROPOSAL.md`](experiments/2026-09-25_gate_fix/PROPOSAL.md)
  - 추석(9/20~22, 월~수)이 이 유형입니다.

---

## 3. 피크 탐지 — 기준선을 먼저 봐야 합니다

달력규칙 한 줄 `평일 ∧ 08≤h≤18` 이 테스트에서 **Recall 1.000** 을 냅니다.

| 구분 | 모델 | Precision | Recall | F1 | TP | FP | FN |
|---|---|---|---|---|---|---|---|
| **기준** | **평일 ∧ 08≤h≤18** | 0.255 | **1.000** | 0.406 | 28 | **82** | 0 |
| 제안 | 피크 직접분류 | — | 1.000 | — | 28 | — | 0 |
| 제안 | 2단계 레짐(3분류) | — | 0.929 | 0.525 | 26 | — | 2 |

**어떤 ML 모델도 Recall 로는 이 규칙을 이길 수 없습니다.** 그래서 주지표를
**"Recall 유지 + 오경보 최소화"(F1/PR-AUC)** 로 재정의하고, 제안모델의 기여를
**"동일 수준에서 FP 82건 → N건"** 으로 정량화합니다.

이 기준 행이 없으면 "Recall 0.9 달성"이 실제보다 대단해 보이는 착시가 생깁니다.

원본 표: `outputs/tables/ch2_peak_detection.csv` (각 행이 쓴 τ 값이 컬럼으로 노출됨)

### 테스트만으로 모델을 구분할 수 없습니다

테스트 피크 28건은 **8개 날짜·16개 연속블록**에만 분포해 유효 표본이 8~16 에 가깝습니다.
Recall 기준 Clopper-Pearson 95% CI 폭이 **0.37** 입니다.
그래서 비교의 주 근거를 **OOF(양성 156건, 테스트의 5.6배)** 로 둡니다.

---

## 4. 무엇이 성능을 만들었나

### 레짐 분리가 기여 1위입니다

Ablation (기준: 최종모델 OOF):

| 제거한 피처군 | MAE 변화 | 해석 |
|---|---|---|
| **레짐분리** | **가장 크게 악화** | 가동/비가동 분포 분리가 이 데이터의 핵심 구조 |
| 생산량·공장인원 | 악화 | 생산계획 정보의 기여 |
| 기상정보 | 거의 변화 없음 (−0.021) | 계절·냉방 정보 (OOF 가 여름에 한정) |
| 공정상태 | 소폭 악화 | 가동 전환 정보 |
| 과거 전력 지연변수 | **개선(!)** — 단 이틀의 착시 | 아래 참조 |

원본 표: `outputs/tables/ch2_ablation.csv`

### 뜻밖의 결과 — 지연변수를 빼면 좋아지는가? (6.6절에서 검증)

과거 전력 지연변수를 제거하면 OOF MAE 가 **−3.131 개선**됩니다. 최종모델을 바꿀 근거인지
6.6절이 **테스트를 보지 않고 미리 정한 기준**으로 판정했습니다. 결과는 **교체하지 않는다** 입니다.

| 변형 | OOF MAE | 오차 상위 2일(8/2·8/3) 제외 | 토요일 MAE | 사전 기준 |
|---|---|---|---|---|
| 최종모델 (44개) | 11.007 | 7.530 | 7.45 | 기준 |
| 과거전력 6개 제거 | 7.876 | 8.143 (+0.61) | 18.96 (+11.50) | 미통과 (정상일 악화) |
| 48시간 전 2개 제거 | 7.560 | 7.758 (+0.23) | 7.50 (+0.05) | 미통과 (Recall −0.083) |
| 168시간 전 2개 제거 | 10.237 | 8.282 (+0.75) | 17.81 (+10.36) | 미통과 (정상일 악화) |
| 24시간 전 2개 제거 | 10.436 | 6.959 (−0.57) | 7.08 (−0.38) | 미통과 (Recall −0.071) |
| 휴무 인지형 지연변수 | 10.106 | 8.595 (+1.07) | 18.04 (+10.58) | 미통과 (정상일 악화) |

- 개선은 **하계휴가 중 평일 이틀(8/2·8/3)** 에서 나옵니다. 1단계 분류기가 이 두 날의 08~17시를
  모두 가동 레짐으로 보내 OOF 절대오차의 **34%** 가 이틀에 몰렸습니다. 지연변수를 빼면 이 이틀이 맞습니다.
- 대신 **토요일(가동·저부하)이 망가집니다.** 지연변수는 정상 운영에 필요합니다.
- 24시간 전 2개만 뺀 변형은 MAE 가 유의하게 좋아지지만(p=0.005) 피크 미탐지가 11건 늘어 떨어졌습니다.
- 44개 변수 개별 제거는 대부분 잡음 폭(난수 변수 추가 시 0.200 kW) 수준이었습니다.
  - 풍속 제거(−0.225)와 peak15 24시간 전 제거(−0.222) 2개는 형식상 ①~④ 를 통과했습니다.
  - peak15 24시간 전 제거는 D2 에서 방향이 뒤집혔습니다(+0.49).
  - 풍속 제거는 D2 에서도 개선 방향(−0.21)이지만 신뢰구간이 0 을 포함합니다.
  - 두 변형 모두 효과가 잡음 폭 바로 위이고, 44개를 동시에 비교해 우연히 기준을 넘을 수 있는 상황이라 교체 근거로 보지 않았습니다.

➡️ 44개 피처를 유지합니다. 개선 대상은 **1단계 분류기의 휴무 판정**입니다(결함 기록: 4번 한계).
원본 표: `outputs/tables/ch2_lag_decomposition.csv` · `ch2_lag_variants.csv` · `ch2_lofo.csv` · `ch2_gate_diagnosis.csv`

### 제안모델의 이점은 '달력이 깨지는 구간'에 집중됩니다

모델 × fold MAE:

| 모델 | fold2 | fold3 | **fold4** | fold5 |
|---|---|---|---|---|
| Seasonal Naive | 7.5 | 24.4 | **69.3** | 8.3 |
| 2단계 레짐(2분류) | 7.3 | 23.9 | **10.4** | 5.9 |

fold4 는 하계휴가와 재가동을 포함합니다. `lag168` 이 붕괴하는 구간에서
레짐을 먼저 분류하면 붕괴를 피합니다. **평균이 아니라 여기서 갈립니다.**

원본 표: `outputs/tables/ch5_fold_mae_matrix.csv`

---

## 5. 피크 저감 — 최대값 목적함수의 성질

### 상위 사건을 하나라도 남기면 절감이 0입니다

| 레버 | 대상 시각 | Δ최대수요 | 기본요금 절감 |
|---|---|---|---|
| **L1a** (계획서 명세) | 08·13시 | **0.0 kW** | **0원** |
| **L1b** | + 11시 | **8.0 kW** | 798,720원 |
| **L1c** | + 09시 | **13.0 kW** | 1,297,920원 |
| L2 고생산 시간대 이동 | — | 0.1 kW | 7,649원 |
| L4a 야간 이전 | — | 0.1 kW | **순절감 −140,969원** |

L1a 가 0인 이유: 최대값 222 kW 4건 중 3건은 08시이지만 **1건(07-19 11시)이 11시**입니다.
청구 피크는 **최대값**이라 이 한 사건이 남으면 절감이 생기지 않습니다.
11시를 덮으면 이번에는 09시 사건(07-21 09시 = 214 kW)이 새 상한이 됩니다.

➡️ **상위 피크 사건을 순차적으로 모두 덮어야 추가 저감이 생깁니다.**

### 물량 이동보다 동시성 분산이 핵심입니다

pooled 회귀(시간 고정효과 포함)의 생산량 계수는 **β ≈ 0.001 kW/생산1단위** 입니다.
20 kW 를 낮추려면 약 2만 단위를 옮겨야 하는데 하루 총 생산량을 넘습니다.

➡️ 이 공장의 peak15 는 생산 **물량**보다 설비 **동시 가동**이 지배합니다.
**생산 일정을 크게 흔들지 않고도 피크를 관리할 수 있다**는 뜻이라 현장에 유리한 결론입니다.

### L4 는 무조건 좋은 레버가 아닙니다

야간 이전은 인건비가 1.5배가 되어 **순절감액이 음수**입니다.
또한 **주말 할증은 이 데이터상 0**(요일별 인건비 분포가 균등)이므로, 실제 특근수당을
파라미터로 노출해 민감도에 포함했습니다.

모든 레버는 **일일 총 생산량을 보존**합니다 — 생산을 줄이는 게 아니라 옮기므로
매출 손실이 없습니다. 단 L4b(주말 이전)는 날짜를 넘으므로 일 단위 보존이 깨지고
표에 `False` 로 정직하게 표시됩니다.

원본 표: `outputs/tables/ch4_levers.csv`, `ch4_scenarios.csv`, `ch4_sensitivity.csv`

---

## 6. 검증 증거

| 검증 | 결과 |
|---|---|
| 단계별 테스트 | **427개 통과** (`bash run.sh test`, 서빙·모델 번들 181개 포함) |
| 누수 탐지기 음성 대조 | 누수를 주입하면 탐지기가 반드시 실패함을 확인 |
| 노트북 Run All | **무오류 완주** 164셀 |
| 재현성 (FAST 동일시드 2회 비교) | **22개 표 · 1,988개 셀 소수 6자리 일치** (최대 절대차 0.0) — 모델 번들 파일 sha256 까지 동일 (같은 OS 기준). 결과표: `outputs/tables/reproducibility_check.csv` |
| 커밋 결과 대비 (`verify-head`) | 최종 FULL 이 커밋된 결과와 **16개 표 수치 변화 0** · 제출 예측 CSV 바이트 동일 (재실행 없음) |
| 모델 번들 | `eval-full-7be5289680f8` / `deploy-full-d8293292a723` — 재로드 예측 = 제출 파일(비트 일치), 재적합 sha 재현 |
| 게이트 1~4 | 전항목 통과 |
| 게이트 5 (6장) | 5/6 — **MAE 유의성만 미달** (위 ①번, 의도적으로 표시) |
| 최종 게이트 | **15/15** 통과 (모델 번들 4항목 포함, `finalize` 가 강제) |
| 개인정보 후스캔 | **0건** |
| 그림 / 표 | 39장 / 114개 |

게이트 5 의 한 항목이 False 인 것은 **버그가 아니라 정직한 표시**입니다.
숨기지 않고 남겨 두었습니다.

---

## 7. 보고서 작성으로 이어가기

`outputs/report_tbd_filled.md` 에 다음이 들어 있습니다.

1. **플레이스홀더 치환표** — 보고서 `[TBD]` 위치별 채움값과 산출 근거
2. **본문 문장 교정** — 실측과 어긋나는 초안 문장과 고칠 내용
3. **plan 대비 정정** — 계획 단계 예상치와 실측이 다른 항목과 그 원인
4. **장별 PASS/FAIL** — 4·5·6장은 `[TBD]` 토큰이 0개라 정규식만으로는
   통째로 비어도 통과하므로, 요건 기반 판정을 별도로 둡니다
5. **사람이 수행할 항목** — 설문 캡처, 발표자료 최종화, 서명, "작성 요령" 블록 삭제 등
