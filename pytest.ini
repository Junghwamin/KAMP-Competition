[pytest]
testpaths = tests
markers =
    slow: 재탐색·재학습이 필요한 느린 테스트 (`-m "not slow"` 로 제외)
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
    ignore::UserWarning
